# Python
__pycache__/
*.pyc
*.pyo

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Java / Maven
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war
*.log
hs_err_pid*

# Node (if added later)
node_modules/
npm-debug.log*
/package-lock.json
dist/
build/

# Env & secrets (never commit real keys)
.env
.env.local
.env.*.local
!.env.example
*.local
application-local.yml
application-prod.yml

# WeChat dev tools
miniapp/project.private.config.json

# Docker local data
mysql_data/
redis_data/

# Cursor / editor caches
.cursor/

# 压测 / 导出临时文件
tmp-export-test.xlsx
*.xlsx.tmp

# 本地开发协作规范（勿提交远端）
docs/开发协作三大原则.md
*.bundle

# 合作伙伴本地交付副本（从 exhibits 同步，不进 Git）
编辑器模块/
模型转换模块/
编辑器模块.zip
模型转换模块.zip

# OBJ→GLB 转换器本地构建产物（不进远端）
exhibits/模型转换/.venv-build/
exhibits/模型转换/dist/
exhibits/模型转换/build/
exhibits/模型转换/*.spec

# build-viewer.mjs --upload 输出（服务器已直接用 exhibits/，本地按需生成即可）
/exhibits-upload/

# 部署脚本的备份（保留 5 份轮转）与工作台实际端口，都是运行期产物
/_deploy_backup/
exhibits/_runtime/studio-port.txt
# push-staging-editor 每次现打的包，勿入库，免得旧包盖掉线上修复
exhibits/_staging-editor-pack/
# build-viewer.mjs --deploy-player 输出（无 .mjs MIME 的测试机用，不入库）
exhibits/deploy-test-server/
exhibits/player.deploy.html

# build-viewer.mjs --upload 临时目录（进程号后缀，成功后会自动删）
/exhibits-upload.staging-*/
/exhibits-upload.backup-*/
