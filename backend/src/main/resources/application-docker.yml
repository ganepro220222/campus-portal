# Docker Compose 环境：数据库/Redis 使用 compose 服务名连接
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/shuyuan?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: dev123456
  data:
    redis:
      host: redis
      port: 6379
      password: dev123456
      database: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  level:
    com.shuyuan: INFO
