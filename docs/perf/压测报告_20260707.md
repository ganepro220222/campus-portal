# 云端书院 · 压测报告

> 日期：2026-07-07  
> 环境：**dev（Docker Compose 本地）**  
> 执行人：开发团队  
> Git 版本：`aa31c08`（main，CI 全绿）

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 应用规格 | 本机 Docker，单容器 Spring Boot（profile=docker） |
| 数据库 | MySQL 8.0 容器（同机） |
| Redis | Redis 7 容器（同机） |
| 后端版本 | `aa31c08` |
| BASE_URL | `http://localhost:8080` |
| 对照文档 | 《交付物与验收标准 V1.2》§五、《工程化与准生产增强方案 V1.1》E3-1 |

**说明：** 本次为 **dev 本地压测归档**；正式验收指标（浏览 P95 < 500ms）须在 **staging**（规格见《资源配置清单_1.3万学生》）复测后终验。

## 2. 并发报名（验收 §五）

**脚本：** `npm run load:enroll`（`scripts/load/enroll-concurrent.mjs`）  
**活动：** id=99，quota=50  
**并发：** 50  
**前置：** 已执行 `sql/patch-loadtest.sql`

### 结果（归档轮次，2026-07-07 17:58）

| 指标 | 值 | 目标 |
|------|-----|------|
| 成功报名 | 50 | ≤ 50 |
| 最终 enrolled_count | 50 / quota 50 | ≤ quota |
| P50 延迟 (ms) | 714 | — |
| P95 延迟 (ms) | 1175 | — |
| 是否超卖 | **PASS** | 不得超卖 |

### 历史轮次（同环境参考）

| 轮次 | 成功报名 | enrolled | P95 (ms) | 超卖 |
|------|----------|----------|----------|------|
| 合并前基线 | 50 | 50/50 | 614 | PASS |
| 合并后 Docker 重建（冷启动） | 50 | 50/50 | 2170 | PASS |
| **归档轮次** | 50 | 50/50 | 1175 | PASS |

### 原始输出（归档轮次）

```
[load] BASE_URL=http://localhost:8080 ACTIVITY_ID=99 CONCURRENCY=50
[load] 压测前 quota=50 enrolled=0
成功报名: 50
报名后 enrolled_count: 50 / quota: 50
延迟 ms — P50: 714  P95: 1175  max: 1210
[load] PASS: 未超卖，并发报名业务响应正常
```

**结论：** 50 并发报名 **业务验收 PASS**（名额不超卖、无重复/异常失败）。延迟随 JVM/容器状态波动，不作为 dev 硬门禁。

## 3. 并发浏览（验收 §五）

**脚本：** `npm run load:browse`（`scripts/load/browse-load.mjs`）  
**并发：** 200，总请求 400  
**接口：** `/news`、`/courses`、`/halls`、`/home/recommends`、`/announcements/active`

### 结果（归档轮次）

| 指标 | 值 | 目标 |
|------|-----|------|
| 总请求 | 400 | — |
| 成功率 | 100%（400/400） | — |
| P50 (ms) | 382 | — |
| P95 (ms) | 689 | **< 500** |
| 结论 | **WARN** | staging 复测 |

### 历史轮次（同环境参考）

| 轮次 | 成功 | P95 (ms) | 结论 |
|------|------|----------|------|
| 合并前基线 | 400/400 | 466 | PASS |
| 合并后 Docker 重建 | 400/400 | 1806 | WARN |
| **归档轮次** | 400/400 | 689 | WARN |

### 原始输出（归档轮次）

```
[browse] BASE_URL=http://localhost:8080 CONCURRENCY=200 TOTAL=400
总请求: 400  成功: 400  失败: 0
延迟 ms — P50: 382  P95: 689  max: 738
目标 P95 < 500 ms
[browse] WARN: P95 超过目标，staging 环境需复测或记录偏差说明
```

**结论：** dev Docker 下接口功能全成功，P95 偶发低于 500ms（基线 466ms），归档轮 689ms **未达正式 P95 目标**，记 **WARN**，上线门禁以 staging 复测为准。

## 4. 偏差说明

| 偏差项 | 原因 | 后续动作 |
|--------|------|----------|
| 浏览 P95 在 dev 超标 | 本机 Docker 单节点、与 MySQL/Redis 同机、无 CDN/生产级 RDS | staging 按《资源配置清单》部署后复测并更新本报告 |
| 报名 P95 波动大 | JVM 冷启动、本地资源竞争 | staging 预热后复测；业务以「不超卖」为 P0 门禁 |
| 环境非 staging | 云资源/域名/OSS 尚未就绪 | P0-3 staging 部署后补 §3 终验段落 |

## 5. 验收对照小结

| 验收项（§五） | dev 归档结论 | 终验状态 |
|---------------|--------------|----------|
| 50 并发报名不超卖 | **PASS** | dev 已满足；staging 复测待办 |
| 200 并发浏览 P95 < 500ms | **WARN** | staging 复测待办 |
| 压测脚本可重复执行 | **PASS** | — |
| 报告归档（交付物 §8） | **PASS** | 本文档 |

## 6. 签字

| 角色 | 姓名 | 日期 |
|------|------|------|
| 执行 | 开发团队 | 2026-07-07 |
| 确认 | （staging 复测后甲方/乙方确认） | 待定 |
