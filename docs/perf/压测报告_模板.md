# 云端书院 · 压测报告

> 日期：YYYY-MM-DD  
> 环境：dev / staging / prod  
> 执行人：  
> Git 版本：`git rev-parse --short HEAD`

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 应用规格 | 例：4C8G ECS |
| 数据库 | 例：RDS MySQL 2C4G |
| Redis | 例：1GB |
| 后端版本 | commit hash |
| BASE_URL | |

## 2. 并发报名（验收 §五）

**脚本：** `node scripts/load/enroll-concurrent.mjs`  
**活动：** id=99，quota=50  
**并发：** 50

### 结果

| 指标 | 值 | 目标 |
|------|-----|------|
| 成功报名 | | ≤ 50 |
| 最终 enrolled_count | | ≤ quota |
| P95 延迟 (ms) | | — |
| 是否超卖 | PASS / FAIL | 不得超卖 |

### 原始输出

```
（粘贴终端输出）
```

## 3. 并发浏览（验收 §五）

**脚本：** `CONCURRENCY=200 node scripts/load/browse-load.mjs`  
**接口：** `/news`、`/courses`、`/halls`、`/home/recommends`、`/announcements/active`

### 结果

| 指标 | 值 | 目标 |
|------|-----|------|
| 总请求 | | |
| 成功率 | | |
| P50 (ms) | | |
| P95 (ms) | | < 500 |
| 结论 | PASS / WARN | |

### 原始输出

```
（粘贴终端输出）
```

## 4. 偏差说明（如有）

（若 P95 超标但可接受，说明原因与后续优化计划。）

## 5. 签字

| 角色 | 姓名 | 日期 |
|------|------|------|
| 执行 | | |
| 确认 | | |
