# 云端书院小程序 · 文案备案对照清单 V1.0

> **编制目的**：对照 `docs/` 内产品文档与小程序实际代码，记录 2026-07 备案口径下的**纯文案调整**（不涉及 UI 视觉资产变更）。
>
> **原则**：以**当前小程序代码**为准；开发文档（如《技术实施方案 V1.2》）中仍沿用「新闻资讯」等旧称处，前端已按备案要求调整，后台/CMS 需另行同步。
>
> **变更范围**：仅修改用户可见字符串、Mock 兜底数据、默认配置文案；**不修改**校徽、书法字、海报顶栏机构名等 VI 资产。

---

## 1. 变更总览

| 类别 | 调整方向 | 说明 |
|------|----------|------|
| 栏目/导航 | 「新闻」→「动态」 | Tab、页面标题、搜索占位等 |
| 内容模块 | 「新闻资讯」→「书院动态」 | 首页入口、列表页、详情页导航 |
| AI 功能 | 「AI 文化问答」→「智能问答」 | 个人中心、独立页、配额提示 |
| AI 助手 | 「书院文化助手」→「书院助手」 | 浮标抽屉、问答页、欢迎语 |
| 成就体系 | 「文化徽章」→「学习徽章」 | 个人中心、徽章列表页 |
| 文创模块 | 「非遗文化好物」→「精品好物」 | 首页 Banner、关于页功能描述 |
| 海报 | 去除「文化分享」等功能性表述 | 默认标题/副标题、预览底部文案 |
| 分类标签 | 「文化传承」→「专题学习」 | Mock 兜底；线上以后台分类为准 |
| 课程默认 | 「文化课程」→「精品课程」 | 接口无分类时的 decorate 兜底 |
| Slogan | 去除「传承中华优秀传统文化」等功能性 slogan | 登录页、关于页底部 |

---

## 2. 刻意保留（不可改 · 学校 VI）

以下内容为**机构标识与视觉规范**，本次及后续备案文案调整均**不得修改**：

| 类型 | 位置 | 保留文案/资产 |
|------|------|---------------|
| 书法校名 | 首页 Header | `/assets/images/home-title-calligraphy.png` |
| 校徽 | 登录页、关于页、海报 | `/assets/images/school-badge.png` |
| 机构全称 | 登录页、关于页、个人中心、隐私页页脚 | `贵州交通职业大学 · 中华文化书院` |
| 版权行 | 关于页、隐私页 | `© 2026 贵州交通职业大学中华文化书院` |
| 海报顶栏 | `packageD/poster/generate.wxml` / Canvas 绘制 | `中 华 文 化 书 院`（含徽记区布局） |
| 小程序名 | `app.json` window | `云端书院` |
| 品牌主标题 | 登录页 | `云端书院`（「书」字金色样式保留） |

---

## 3. 逐文件对照明细

### 3.1 全局配置

| 文件 | 字段/位置 | 调整前 | 调整后 |
|------|-----------|--------|--------|
| `miniapp/app.json` | TabBar `pages/news/index` | 新闻 | 动态 |
| `miniapp/app.json` | `permission.scope.writePhotosAlbum.desc` | 保存文化海报到相册 | 保存分享海报到相册 |
| `miniapp/custom-tab-bar/index.js` | Tab 文案 | 新闻 | 动态 |

### 3.2 首页 `pages/index/`

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `index.wxml` | 搜索占位 | 搜索新闻、课程、展馆、资源… | 搜索动态、课程、展馆、资源… |
| `index.wxml` | 快捷入口 | 新闻资讯 | 书院动态 |
| `index.wxml` | 文创 Banner 标题 | 非遗文化好物 | 精品好物 |
| `index.wxml` | 区块标题 | 最新资讯 | 最新动态 |
| `index.wxml` | 轮播默认分类 | （兜底） | 书院动态（`decorate.js`） |

### 3.3 动态模块（原「新闻资讯」）

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `pages/news/index.wxml` | 导航标题 | 新闻资讯 | 书院动态 |
| `pages/news/index.wxml` | 空状态 | 暂无资讯 | 暂无动态 |
| `packageA/news/list.json` | `navigationBarTitleText` | 新闻资讯 | 书院动态 |
| `packageA/news/list.wxml` | 空状态 | 暂无新闻 | 暂无动态 |
| `packageA/news/detail.wxml` | `nav-bar title` | 新闻资讯 | 书院动态 |

> **说明**：路由路径仍为 `pages/news/*`、`packageA/news/*`，仅改展示文案，不改代码结构。

### 3.4 智能问答（原「AI 文化问答」）

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `pages/profile/index.wxml` | 菜单项 | AI 文化问答 | 智能问答 |
| `packageD/ai-chat/index.wxml` | 导航标题 | AI 文化问答 | 智能问答 |
| `packageD/ai-chat/index.wxml` | 助手名称 | 书院文化助手 | 书院助手 |
| `packageD/ai-chat/index.wxml` | 输入占位 | 向书院文化助手提问… | 向书院助手提问… |
| `components/ai-assistant/index.wxml` | 抽屉标题 | 书院文化助手 | 书院助手 |
| `components/ai-assistant/index.wxml` | 输入占位 | 向书院文化助手提问… | 向书院助手提问… |
| `utils/miniappConfig.js` | `aiAssistantWelcome` | …书院文化助手…文化相关问题 | …书院助手…学习相关问题 |
| `utils/aiChat.js` | 未登录提示 | …书院文化助手 | …书院助手 |
| `utils/aiChat.js` | 配额副标题 | （新增统一口径） | 登录后可使用 AI 智能问答 |

### 3.5 个人中心与徽章

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `pages/profile/index.wxml` | 菜单项 | 文化徽章 | 学习徽章 |
| `packageC/profile/list.js` | `badges.title` | 文化徽章 | 学习徽章 |

### 3.6 全局搜索

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `packageC/search/index.wxml` | 输入占位 | 搜索新闻、课程、展馆、资源… | 搜索动态、课程、展馆、资源… |
| `packageC/search/index.js` | Mock 结果 `typeLabel` | 新闻 | 动态 |

### 3.7 文创展示

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `packageA/craft/list.wxml` | 列表页引言（中文） | 非遗匠心 · 文化好物 | 匠心精品 · 好物推荐 |
| `packageA/craft/list.wxml` | 列表页引言（英文） | Intangible Cultural Heritage · Creative Works | Selected Creative Works |
| `packageA/craft/detail.wxml` | 分享按钮 | 一键生成文化分享海报 | 一键生成分享海报 |
| `packageA/hall/detail.wxml` | 分享按钮 | 一键生成文化分享海报 | 一键生成分享海报 |

> **说明**：页面导航标题「文创展示」与 docs 一致，未改。

### 3.8 分享海报

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `packageD/poster/generate.js` | 默认 `title` | 云端书院 · 文化分享 | 云端书院 · 分享 |
| `packageD/poster/generate.js` | 默认 `subtitle` | 传承中华优秀传统文化 | 扫码进入云端书院 |
| `packageD/poster/generate.wxml` | 预览底部 `pf-2` | 云端书院 · 传承中华文化 | 扫码进入云端书院 |
| `packageD/poster/generate.js` | Canvas 顶栏 | 中 华 文 化 书 院 | **保留不变** |
| `packageD/poster/generate.js` | Canvas 底栏 | 长按识别小程序码 · 云端书院 | **保留不变** |

### 3.9 登录页

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `pages/login/index.wxml` | `brand-sub` | 贵州交通职业大学 · 中华文化书院 | **保留不变** |
| `pages/login/index.wxml` | `foot-b` | “马院 + 书院”协同育人 · 云端思政平台 | **保留不变** |
| `pages/login/index.wxml` | `foot-s` | 线上线下相结合 · 传承中华优秀传统文化 | 云端学习平台 · 线上线下相结合 |

### 3.10 关于页

| 文件 | 位置 | 调整前 | 调整后 |
|------|------|--------|--------|
| `packageC/about/index.js` | `intro`（兜底） | …阳明文化、屯堡文化…传承中华优秀传统文化… | …整合线上展馆、精品课程…线上线下相结合 |
| `packageC/about/index.js` | 功能「线上展馆」desc | 11 座沉浸式文化馆 | 沉浸式线上展馆，图文 + 语音讲解 |
| `packageC/about/index.js` | 功能「文化课程」 | 文化课程 / 阳明心学、红色文化… | 精品课程 / 多门精品课程与 AI 字幕 |
| `packageC/about/index.js` | 功能「文创展示」desc | 非遗好物双语鉴赏… | 精品好物双语鉴赏… |
| `packageC/about/index.js` | 功能「AI 文化问答」 | AI 文化问答 | 智能问答 |
| `packageC/about/index.wxml` | `ab-slogan` | 传承中华优秀传统文化 · 培根铸魂启智润心 | “马院 + 书院”协同育人 · 云端思政平台 |
| `packageC/about/index.wxml` | `ab-org` / `ab-copy` | 机构名与版权 | **保留不变** |

> **说明**：简介正文优先读取后台 `GET /config/about`，上表为接口不可用时的兜底文案。

### 3.11 Mock 兜底数据 `mock/defaults.js`

| 数据项 | 调整前 | 调整后 | 备注 |
|--------|--------|--------|------|
| 动态分类筛选项 | …文化传承 | …专题学习 | 与「书院动态」「活动通知」并列 |
| 课程分类筛选项 | …文化传承 | …专题学习 | 「阳明文化」等内容分类保留 |
| 动态/课程条目 `category` / `categoryName` / `cat` / `tag` | 文化传承 | 专题学习 | 仅 Mock 展示 |
| 资源 `categoryName` | 文化读本 | 学习读本 | |
| 文章标题、详情正文 | 含「中华文化书院」等机构名 | **保留不变** | 内容为机构真实信息 |

### 3.12 接口兜底 `utils/decorate.js`

| 函数 | 字段 | 调整前 | 调整后 |
|------|------|--------|--------|
| `decorateNews` / 相关 | `categoryName` 默认 | （各类） | 书院动态 |
| `decorateCourses` | `categoryName` 默认 | 文化课程 | 精品课程 |
| `decorateCourseCards` | `tag` 默认 | 文化课程 | 精品课程 |

---

## 4. 与 docs 文档的差异说明

| docs 文档表述 | 小程序现行口径 | 处理建议 |
|---------------|----------------|----------|
| 《技术实施方案 V1.2》「新闻资讯」模块 | 前端称「书院动态」/ Tab「动态」 | 文档可后续修订；以线上小程序为准 |
| 《交付物与验收标准 V1.2》「新闻管理」 | 后台功能名可保留；前端展示改「动态」 | 验收时以小程序 UI 文案为准 |
| 《管理员操作手册》新闻发布流程 | 后台菜单/字段名未改 | 运营发布内容时注意分类名同步 |
| docs 中「AI 文化问答」 | 前端「智能问答」 | 后台知识库模块名可不变 |

---

## 5. 后台 / CMS 待同步项（非本次代码范围）

以下若仍使用旧文案，**线上会与 Mock 兜底不一致**，建议运营/后台一并调整：

| 配置项 | 建议值 | 配置入口（参考） |
|--------|--------|------------------|
| 动态分类名 | 书院动态、活动通知、专题学习 | 后台 · 分类管理 |
| 小程序公开配置 `aiAssistantWelcome` | 与 `miniappConfig.js` 一致 | 后台 · 内容配置 |
| 关于页 `intro` | 与 `about/index.js` 兜底一致 | 后台 · 内容配置 / `GET /config/about` |
| 隐私政策 / 用户协议正文 | 避免「新闻发布」等功能性违规表述 | 后台 · 协议内容 |
| 文章正文标题 | 机构活动真实标题可保留「文化」字样 | 内容审核口径 |

---

## 6. 未修改项（内容性「文化」字样）

以下内容**属于课程/活动/展馆真实名称或正文**，非功能模块命名，本次** intentionally 保留**：

- Mock 课程名：如「阳明心学十二讲」「屯堡文化探源」
- Mock 动态标题：如「中华文化书院举办…专题讲座」
- 展馆名：如「文化千岛艺术馆」
- 课程分类：「阳明文化」（专题维度，非 Tab 名）
- 搜索热词 / AI 示例问题：如「什么是阳明文化？」（用户提问示例）
- 展馆分类：「文化艺术」等（来自校方展馆体系）

若备案审核对**正文内容**另有要求，需单独梳理 CMS 已发布文章，不在本清单代码变更范围内。

---

## 7. 变更文件清单（Git）

```
miniapp/app.json
miniapp/custom-tab-bar/index.js
miniapp/components/ai-assistant/index.wxml
miniapp/mock/defaults.js
miniapp/packageA/craft/detail.wxml
miniapp/packageA/craft/list.wxml
miniapp/packageA/hall/detail.wxml
miniapp/packageA/news/detail.wxml
miniapp/packageA/news/list.json
miniapp/packageA/news/list.wxml
miniapp/packageC/about/index.js
miniapp/packageC/about/index.wxml
miniapp/packageC/profile/list.js
miniapp/packageC/search/index.js
miniapp/packageC/search/index.wxml
miniapp/packageD/ai-chat/index.wxml
miniapp/packageD/poster/generate.js
miniapp/packageD/poster/generate.wxml
miniapp/pages/index/index.wxml
miniapp/pages/login/index.wxml
miniapp/pages/news/index.wxml
miniapp/pages/profile/index.wxml
miniapp/utils/aiChat.js
miniapp/utils/decorate.js
miniapp/utils/miniappConfig.js
docs/小程序文案备案对照清单_V1.0.md
```

---

## 8. 验收自检清单

- [ ] 底部 Tab 显示「动态」而非「新闻」
- [ ] 首页快捷入口为「书院动态」「精品好物」「最新动态」
- [ ] 个人中心为「智能问答」「学习徽章」
- [ ] AI 浮标与独立页助手名为「书院助手」
- [ ] 登录页、关于页底部无「传承中华优秀传统文化」 slogan
- [ ] 海报预览顶栏仍为「中 华 文 化 书 院」，底部为「扫码进入云端书院」
- [ ] 校徽、书法字、机构全称视觉与改前一致
- [ ] 对接真实后台后，分类名与关于页简介与备案口径一致
- [ ] 「智能问答」页与「书院助手」抽屉输入框下方常驻「内容由 AI 生成，仅供参考」，且未被 Home Indicator 遮挡
- [ ] 海报预览底部两行 = 保存下来的图片底栏文案，无「扫码 / 长按」冲突
- [ ] 首页文创 Banner 无「选购」等电商引导表述
- [ ] 隐私政策与用户协议页有正文；后台未配置时显示来源提示与「重新加载最新版本」，不出现空白卡片
- [ ] 海报预览小程序码占位框显示「小程序码 / 保存时生成」两行，无 `\n` 字样、不溢出方框
- [ ] `npm run preflight:local` 全绿（含 `check:wxml` 与 `test:miniapp-utils`）
- [ ] 带封面的海报：用一个 11 字以上的馆名/展品名生成，副标题不得压到二维码上，且预览与保存下来的图一致
- [ ] 下载记录点「再次下载」，芯片宽度不变、左侧标题不重排
- [ ] 首页功能入口卡左右缩进与轮播/精品好物一致，不贴屏幕边
- [ ] 后台把首页入口配成 6 条以上：第 6 条起换行，图标不变形；配一个 6 字以上的名字应显示省略号
- [ ] 文创/展馆详情的收藏胶囊右边缘与联系卡片对齐；点击时宽度不变
- [ ] 资源列表里收藏与下载两颗圆钮同样大小
- [ ] 后台侧栏 26 个菜单项图标互不重复（「分类管理」是标签形、「功能入口」是九宫格）
- [ ] 各页可点胶囊按下去都是「变深」，没有反过来变浅的

---

## 9. UI 视觉补充轮（V1.1）

V1.0 只动了字符串，声明「不涉及 UI 视觉」。落地后复查发现有四处**属于视觉呈现层面**的问题：
两处是 V1.0 改文案带出来的（改了预览没改成品、改了措辞留下电商暗示），
两处是 V1.0 范围之外、但审核员必看的界面缺口（AI 生成标识、协议页空白）。本轮一并处理。

### 9.1 AI 生成内容标识（新增 · 优先级最高）

《生成式人工智能服务管理暂行办法》第十二条与《互联网信息服务深度合成管理规定》要求在**界面显著位置**标识生成内容；
微信小程序对 AI 问答类功能按同口径审核。原「智能问答」页与「书院助手」浮标抽屉**均无任何标识**。

| 文件 | 变更 |
|------|------|
| `packageD/ai-chat/index.wxml` / `.wxss` | 输入行外包 `.chat-foot`，其下常驻 `.ai-note`「内容由 AI 生成，仅供参考」 |
| `components/ai-assistant/index.wxml` / `.wxss` | 抽屉输入行外包 `.sheet-foot`，同一条标识 |

实现要点：分隔线与 `env(safe-area-inset-bottom)` 从输入行**上移到外层容器**，
标识落在安全区之上而不是被 Home Indicator 压住；两处均 20rpx / `--muted`，
沿用业界（豆包 / 元宝一类）输入框下方常驻灰字的写法。底栏增高 ≈14–34rpx，
由 `flex:1` 的消息区吸收，不改变任何既有间距。

### 9.2 分享海报：预览与成品不一致

V1.0 把**预览**底部 `pf-2` 改成「扫码进入云端书院」，但 `_render()` 里 Canvas 实际绘制的底栏
仍是「长按识别小程序码 · 云端书院」（V1.0 §3.8 标注为保留不变）。海报是保存到相册、
转发到微信的**对外物料**，"所见即所得"被打破；且同一张图上「扫码」与「长按识别」互相矛盾——
图片在微信里的正确识别方式是长按。

| 文件 | 调整前 | 调整后 |
|------|--------|--------|
| `packageD/poster/generate.wxml` `pf-2` | 扫码进入云端书院 | 云端书院（与 Canvas 底栏逐字对应，两行拼起来即「长按识别小程序码 · 云端书院」） |
| `packageD/poster/generate.js` 默认 `title` | 云端书院 · 分享 | 云端书院 |
| `packageD/poster/generate.js` 默认 `subtitle` | 扫码进入云端书院 | 线上展馆 · 精品课程 · 文创展示 |

副标题改为功能描述（三个词均取自备案后的模块名），不再与底栏重复喊同一句 CTA。
排版核算：Canvas 副标题 13px × 12 汉字 + 2 分隔 ≈ 194px，内框宽 272px；
预览 `.p-sub` 22rpx 计 ≈ 366rpx，可用宽 400rpx；`title` 默认 4 字，两处均单行不折行。

### 9.3 首页文创 Banner「选购」措辞

`pages/index/index.wxml` 副标题「工艺品鉴赏 · 咨询选购」——站内无任何交易环节
（详情页只有「合作与咨询」+ 电话），「选购」属电商引导表述，与教育类目不符。

| 调整前 | 调整后 |
|--------|--------|
| 工艺品鉴赏 · 咨询选购 | 工艺品鉴赏 · 合作咨询 |

同为 4 字，`.craft-sub` 无固定宽度，排版零影响。

### 9.4 隐私政策 / 用户协议页空白兜底

`packageC/legal/privacy.*` 正文 100% 来自后台 `GET /config/documents`，
接口未配置或不可用时渲染出**两张只有标题的空白卡片**——审核必点此页，空白即驳回。

| 文件 | 变更 |
|------|------|
| `packageC/legal/privacy.wxml` | `rich-text` 加 `wx:if`，为空时显示 `.legal-empty` 提示 |
| `packageC/legal/privacy.wxss` | 新增 `.legal-empty` / `.legal-retry` 样式 |
| `packageC/legal/privacy.js` | 抽出 `load()`，新增 `onRetry()` 重新拉取 |

> 这只修掉「空白卡片」这个视觉缺陷。**正文仍必须由后台配置**，否则审核照样过不了——见 §5。

### 9.5 关于页功能卡描述去重

`packageC/about/index.js`：「线上展馆 / 11 座沉浸式**线上展馆**…」「精品课程 / 多门**精品课程**与 AI 字幕」
名称与描述同词重复，卡片读起来啰嗦。改为「11 座沉浸式展馆，图文 + 语音讲解」「多门在线课程，支持 AI 字幕」。
`.feat-desc` 为 `flex:1; min-width:0` 自由折行，长度无约束。

### 9.6 本轮排查但**未**改动的项

| 项 | 结论 |
|----|------|
| TabBar / 快捷入口图标（`news`、`entry-news` 报纸样式）与文案「动态」 | 报纸类图标表示信息流是通行画法，不构成图文不符，保留 |
| 各处缩短后的文案（快捷入口、搜索占位、海报按钮、Slogan） | 逐条核过对应 WXSS：均为块级或 `flex:1 + min-width:0`，无固定宽度、无 `nowrap` 溢出风险；`.entry-label` 虽 `nowrap` 但新旧同为 4 字 |
| 关于页 `ab-slogan` 与登录页 `foot-b` 现文案相同 | 分属两页页脚，不同屏不同时可见，非缺陷 |
| 校徽 / 书法字 / 机构全称 / 版权行 / 海报顶栏 | 按 §2 一字未动 |

### 9.7 §5 后台待同步项补充

| 配置项 | 建议值 | 说明 |
|--------|--------|------|
| `GET /config/documents` 的 `privacy` / `agreement` | 必填 | 为空时前端只能显示 §9.4 的空状态，审核必驳 |
| 隐私政策正文 | 需含 AI 问答相关条款 | 说明提问内容的收集、用途、留存期限与第三方模型调用情况 |
| 生成式 AI 服务备案 | 视上线主体确认 | 若「智能问答」对公众开放，需核对是否需完成算法/大模型备案；§9.1 的界面标识是必要条件，不是充分条件 |

---

## 10. UI 视觉第二轮（V1.2 · 收尾批次）

对应上游 `1333adf`…`6609173`（A1–A6 功能收尾 + 协议兜底 + 海报小程序码）。
逐个页面复查后，**只有一处真正的视觉缺陷**，其余新增界面核对无误。

### 10.1 修复：海报预览的小程序码占位框显示出 `\n`

`packageD/poster/generate.wxml` 把假二维码换成占位框是对的（对外物料里画假码本就不该有），
但占位文案写成了 `保存时将加载\n小程序码` —— **WXML 文本节点不解析反斜杠转义**，
`\n` 会原样出现在屏幕上。且 `.p-qr` 只有 96rpx 见方，16rpx 字号下「保存时将加载」正好 96rpx，
即便换行生效也会被挤成三四行顶破方框。

改为两个 `<text>` 分行，并核算过宽度：

| | 内容 | 宽度 | 可用 |
|---|---|---|---|
| 第一行 | 小程序码 | 4 × 17rpx = 68rpx | 94rpx |
| 第二行 | 保存时生成 | 5 × 17rpx = 85rpx | 94rpx |

汉字 advance 恒为 1em，宽度是确定值，因此加 `white-space: nowrap` 锁死两行；
两行合计 47.6rpx，在 96rpx 框内垂直居中。

### 10.2 已核对、无需改动的新增界面

| 界面 | 核对结论 |
|------|----------|
| 动态详情「分享」由 `<view>` 改 `<button open-type="share">` | 按钮 UA 默认样式（`display:block` / `#f8f8f8` 底 / 18px 字 / `::after` 边框 / `button-hover`）逐条都被 `.act` `.act-share-btn` 的类选择器盖住（类 0,1,0 胜过类型 0,0,1），与左右两个 `<view>` 同高同底同字号；`detail.json` 已开 `enableShareAppMessage` / `enableShareTimeline`，`onShareAppMessage` / `onShareTimeline` 都在，不是死按钮 |
| 「问答历史」新页 | `nav-bar` 自带等高 `.nb-holder` 占位，页面级滚动不会被固定顶栏压住；`.history-title` 单行省略号；空状态与列表态互斥 |
| 报名凭证二维码 | `.result-card` 是 `text-align:center` 的 flex 列，提示文字居中正常；320rpx 显示 / 480px 导出，模块尺寸足够扫；离屏 canvas 用 `left:-9999px` 而非 `display:none`，不影响 `canvasToTempFilePath` |
| 动态列表触底加载 | 用内层 `scroll-view` 的 `bindscrolltolower`（页面用的是容器滚动，`onReachBottom` 本就不会触发），`.list-foot` 加载中/没有更多两态互斥 |
| 个人中心「问答历史」入口 `<icon name="chat">` | `icons.js` 里已定义，不会渲染成空白 |
| 协议页 `.legal-hint` 来源提示条 | 样式与卡片同一套圆角/描边，不突兀 |
| 课程播放页首行 `// …` 当正文渲染 | 上游本轮已修（改成 `<!-- -->`），全仓再无同类残留 |

### 10.3 新增两道 preflight 关卡

本轮两处缺陷（`\n` 字面量、`//` 当正文）都属于「肉眼扫代码扫不出、跑起来才看见」的类型，
而后续还有第二批、第三批。补两个脚本接进 `npm run preflight:local`：

| 脚本 | 拦什么 |
|------|--------|
| `scripts/check-wxml.js` | ① 文本节点里的 `\n` / `\t` / `\r` 字面转义（`{{ }}` 内是 JS 字符串，跳过）② 文本节点里的 `//` 与 `/* */` ③ `<icon name>` 引用 `icons.js` 未定义的图标 ④ 标签闭合不匹配 |
| `scripts/test-miniapp-utils.js` | 统一执行 `miniapp/utils/*.test.js`。这 8 个用例文件此前没有任何脚本会运行它们 |

四条规则都做过反向验证：把三处历史缺陷（`\n`、`//` 首行、不存在的图标名）和一处人为的
未闭合标签放回去，`check-wxml` 逐条报红；还原后恢复绿灯。

### 10.4 需要运营/后台确认的两件事

| 项 | 说明 |
|----|------|
| 内置基线协议正文里的「（内置基线，运营后台配置后将自动更新）」 | 这句写在「隐私政策」标题行上，配合页面顶部的来源提示条，审核员会看到两处「这是占位」的暗示。**正式提审前请在后台配好 `/config/documents` 正文**，让来源变成 remote，提示条与该括注都不再出现 |
| 基线正文的法律措辞 | `utils/legalDocuments.js` 里的 BASELINE 是完整可读的一版，但属于法律文本，需由校方/运营方核准后再作为对外版本，不应由开发侧定稿 |

---

## 11. UI 视觉第三轮（V1.3 · A7–A13 批次）

对应上游 `797db6e`…`a6de6ec`（学习足迹时间轴 / 下载记录再次下载 / 海报嵌封面 / 反馈附图 / 收藏标签）。
`check:wxml` 与 `test:miniapp-utils` 两道关卡本轮**开箱全绿**，说明第二轮补的规则起作用了；
剩下的是它们查不到的「几何算错」，找到一处严重的。

### 11.1 修复（严重）：海报嵌封面后，副标题画在二维码上

A9 给海报加了封面图，标题起始基线随之从 250 挪到 318 —— 只顾了「让标题离封面远一点」，
没有回头核算下边界。画布 300×500 的实际排版是：

```
副标题基线 = titleStartY + 32 × 标题行数 + 34
二维码白底板     y = 386 起
标题最多 3 行（drawRest 里 lines.slice(0, 3)）
```

| titleStartY | 1 行 | 2 行 | 3 行 |
|---|---|---|---|
| 250（无封面，原有） | 316 ✓ 余 67px | 348 ✓ 余 35px | 380 ✓ 余 3px |
| **318（有封面，本轮前）** | 384 ✗ 越界 1px | 416 ✗ **越界 33px** | 448 ✗ **越界 65px** |
| **235（有封面，已修）** | 301 ✓ 余 82px | 333 ✓ 余 50px | 365 ✓ 余 18px |

无封面那条 250 是照 3 行卡出来的（只剩 3px 余量，明显是手调过的）；有封面这条则是**每种行数都撞**。
标题换行宽度 220px、字号 22px，约 10 个汉字一行 —— 也就是说**标题超过 10 个字就会把副标题
画到二维码上**。「贵州交通职业大学校史馆」这类馆名（11 字）直接命中。海报是分享出去给人扫的，
糊住码等于这张图作废。

235 按上边界定：封面 y 58..182，右下角徽记圆底部到 195，235 的墨迹顶部约 213，留 18px。
无封面路径一个像素没动，观感零回归。

顺带修正了 WYSIWYG：改之前画布把标题压到离封面 136px 的位置，而预览里 `.p-title-cover`
只留 36rpx（折算约 22px）—— 两边本来就对不上；改后画布是 53px、折算 ≈ 34rpx，与预览基本一致。

### 11.2 修复：预览标题不封顶，长标题会把二维码那一行挤出画面

画布是 `lines.slice(0, 3)`，预览 `.p-title` 却没有行数上限。480×800rpx 的预览里，
封面模式排到第 4 行就是 806rpx > 800rpx，`.poster` 是 `overflow:hidden`，底部二维码那一行
直接被切掉，而存下来的图只有 3 行 —— 预览即成品不成立。给 `.p-title` 加 3 行 `-webkit-line-clamp`
与画布对齐（该属性本仓已在 5 处使用，非新引入）。

### 11.3 修复：「再次下载」芯片点一下会抖

`.pl-redownload` 文案在「再次下载」(4 字 = 88rpx) 与「下载中」(3 字 = 66rpx) 之间切换，
而 `.pl-right` 是 `flex-shrink:0`、`.pl-body` 是 `flex:1` —— 芯片缩 22rpx，左边两行截断的
标题跟着重排。加 `min-width:128rpx` + `box-sizing:border-box` + 居中，按长的那个态定死下限。

### 11.4 把「钉数字」的测试改成「核算几何」

`posterCover.test.js` 原本写的是 `assert.strictEqual(titleStartY(true), 318)` ——
**数字断言得好好的，画出来却压在二维码上**。这类测试只能防手滑改动，防不住算错。
改成按 `_render` 的真实排版复算副标题位置，断言 1/2/3 行都落在白底板上方，
另加一条断言标题不骑到封面右下角的徽记圆上。

反向验证：把 318 放回去，测试报 `有封面 1 行标题：副标题底部 387 压到二维码白底板（386）`；
改回 235 后恢复绿灯。

### 11.5 已核对、无需改动

| 界面 | 核对结论 |
|------|----------|
| 学习足迹时间轴 | `.fp-rail-dot` 的 `left:-31rpx` 与 `.fp-day-list` 的 `border-left` 精确重合（圆心 -25，线心 -25）；`.fp-card` 有 `flex:1 + min-width:0`，长标题不撑破 |
| 下载记录行 | `.pl-right` 没设 `gap`，但「再次下载」芯片与「已失效」状态由 `canRedownload` 保证互斥，`mapDownloadRecordItem` 也不给 `route`，所以右侧永远只有一个子元素，不会贴在一起 |
| 反馈附图九宫格 | 卡片内宽 638rpx（750 − 边距 56 − 内边距 56），3 × 168 + 2 × 16 = 536 放得下、4 个放不下 → 稳定 3 列，9 张正好 3×3 |
| 动态详情点赞/收藏文案切换 | 「收藏」↔「已收藏」虽是 2↔3 字，但 `.act` 是 `flex:1` 三等分，按钮宽度不随文案变 |
| `.fp-rail-line` | 元素与样式都在，但 `display:none`，竖线实际由 `.fp-day-list` 的 `border-left` 画。是冗余不是缺陷，未动 |

---

## 12. UI 视觉第四轮（V1.4 · B1–B3 批次）

对应上游 `66b7ab3`…`f5bb3b2`（多模块收藏 / 首页入口后台可配 / 个人资料编辑）。
两道关卡开箱全绿，功能实现本身没问题；本轮找的是**风格一致性与几何**，共五处。

### 12.1 首页入口卡是全场唯一贴边的卡片（存量问题，本轮一并修）

元素写的是 `class="entries-wrap wrap"`，本意是套用 `.wrap` 的 `padding: 0 32rpx` 缩进。
但 `.entries-wrap` 在文件里排在 `.wrap` 之后、specificity 又相同，它自己的
`padding: 30rpx 12rpx 24rpx` 把横向缩进**整个盖掉**了。

逐个核对了页面里所有带 `wrap` 的元素：

| 元素 | 是否自带 padding | 结果 |
|------|------------------|------|
| `.wrap`（轮播） | 否 | 缩进 32rpx |
| `.craft-banner wrap` | 否 | 缩进 32rpx |
| `.section wrap` | 否 | 缩进 32rpx |
| **`.entries-wrap wrap`** | **是** | **贴屏幕边，0 缩进** |

也就是说这张带 1rpx 描边、28rpx 圆角和投影的白卡，是首页唯一一个左右贴边跑的——
两侧圆角和阴影全被压在屏幕边缘上。缩进改用 `margin: 24rpx 32rpx 0` 表达，不再和 padding 打架。

> 这一处是存量问题、不是 B2 引入的，但它改变了首页可见布局。若你们本来就想要贴边效果，
> 回退这一条即可，其余四处互不影响。

### 12.2 首页入口矩阵撑不住「后台可配」

B2 把这排入口改成后台配置（条数、图标、文案都可变），但样式还是按写死的 5 个 4 字词写的：
`.entry { flex: 1 }` 均分 + `.ico` 死宽 100rpx + `.entry-label` 是 `nowrap` 且无省略号。

| 条数 | 改前每格宽 | 结果 |
|---|---|---|
| 5 | 134rpx | ✓ |
| 6 | 111.7rpx | 勉强 |
| 7 | 95.7rpx | ✗ 图标 100rpx 顶出格子 |
| 8 | 83.8rpx | ✗ 同上 |

文案同理：5 字以上（115rpx）在 6 条时就会和邻格叠在一起。

改成**固定 5 列换行**（`flex: 0 0 20%` + `flex-wrap` + `gap: 26rpx 0`），
第 6 条起自然落到第二行，每格恒为 132.4rpx；标签加 `max-width:100%` + 省略号。
列间距必须为 0，否则 5×20% 加间距就挤不下 5 列。

### 12.3 收藏胶囊比同页其他内容多缩进一个 28rpx

`.fav-bar` 写的是 `margin: 0 28rpx 8rpx`，但三处调用（文创 / 展馆 / 课程详情）
都在 `.detail-wrap { padding: 24rpx 28rpx }` 里面 —— 横向缩进被叠加成 56rpx，
右对齐的胶囊右边缘对不齐上方的联系卡片和下方的海报按钮。
横向留给容器，改成 `margin: 24rpx 0 8rpx`（顺带补上原本为 0 的上边距）。
课程详情用的 `.fav-bar-inline` 本来就把横向margin 覆盖成 0，不受影响。

### 12.4 收藏胶囊点一下会抖

和上一轮「再次下载」同一类：「收藏」(2 字) ↔「已收藏」(3 字) = 25rpx 宽度差，
而胶囊是右对齐的，每点一次左边缘蹦一下。按长的那个态锁下限：
图标 32rpx + gap 8rpx + 3×25rpx + 28rpx×2 = 171rpx → `min-width: 172rpx` + 居中。

### 12.5 资源列表两颗圆钮一大一小

`.fav-icon`（56rpx，`--sky-3` 底 + 1rpx 描边）与 `.res-dl`（72rpx，`--sky-2` 底、无描边）
在 `.res-actions` 里并排、`align-items:center`。同一行两颗正圆差 16rpx，一个带描边一个不带，
看着像没对齐。`.fav-icon` 跟 `.res-dl` 取齐：72rpx + `--sky-2` + 无描边，
选中态仍用红系（`#FDF1EF`）区分，并补 `:active` 反馈。

### 12.6 已核对、无需改动

| 项 | 结论 |
|----|------|
| 个人资料编辑页（B3） | 结构、间距、输入框样式与「意见反馈」页几乎逐条对应，风格一致；`.pe-label:not(:first-of-type)` 因为首个 `view` 是 `.pe-hint`，第一个标签也会拿到 26rpx 上边距，与 `.pe-hint` 的 24rpx 下边距合并后为 26rpx，无碍 |
| 收藏「选中」配色 | `#C0392B` / `#F3C9C2` / `#FDF1EF` 与动态详情既有的 `.act-on` 完全一致，是同一套语言 |
| 详情页胶囊右对齐 vs 课程页左对齐 | 前者是两个内容块之间的独立操作条，后者在信息卡内随正文左对齐，属于不同容器的合理差异，未统一 |
| `mergeHomeNavItems` 不限条数 | 换行后 N 条都能排好，不需要再加上限，把条数交给后台 |

### 12.7 建议你们在开发者工具里确认一条（我这边无法验证）

全项目 7 个按钮用了 `disabled="{{...}}"` 配自定义底色（登录 3 个、报名、反馈、个人资料、海报保存）。
微信基础库带 `button[disabled]:not([type]) { color: rgba(0,0,0,.3) }`，
specificity (0,2,1) 高于 `.pe-submit.ing` 这类 (0,2,0) 的写法 ——
**提交中的按钮文字（「保存中…」「生成中…」）可能变成 30% 黑压在深蓝渐变上，几乎看不见**。

这是存量的、跨页面的写法问题，不是本批引入；我没有模拟器无法确认基础库当前实际样式，
所以只报不改。若确认属实，统一加一条 `.xxx[disabled] { color: #fff; }` 即可（specificity 打平且在后，胜出）。

---

## 13. UI 视觉第五轮（V1.5 · 稳定性批次 + 后台侧栏图标）

对应上游 `99eefaf`…`eefc019`（12 个提交：错误态、字段校验、上传响应、列表刷新）。
**上一轮 §12.7 报的 disabled 按钮问题已被采纳修复**（`app.wxss` 里
`button.xxx[disabled] { color: #fff }`，specificity 打平且在后，写法正确），
各页 `line-height: Npx` 也统一换成了 flex 居中 —— 这是对的，`line-height = height`
在基础库的内边距下本来就会让文字偏下。

### 13.1 修复：后台侧栏「分类管理」与「功能入口」是同一个图标

`admin/src/router/index.ts` 里两处都写了 `Grid`，侧栏上就是两个一模一样的九宫格，
只能靠读文字分辨。两处相隔 10 行、分属「内容运营」与「首页配置」两个分组，扫代码很难发现。

| 菜单项 | 原图标 | 现图标 | 理由 |
|---|---|---|---|
| 功能入口 | `Grid` | `Grid`（保留） | 它管的就是首页那个九宫格入口矩阵，语义最贴 |
| 分类管理 | `Grid` | `PriceTag` | 标签形，是「分类 / 标签」的通行画法，且全表再无第二个标签形 |

顺带处理一对**近似重复**：「意见反馈」用 `ChatDotRound`、「AI 助手配置」用 `ChatDotSquare`
—— 同为带三个点的对话气泡，只差圆角，侧栏 16px 尺寸下分不出来。
后者改 `Operation`（三条滑杆 = 配置），与「系统管理」的 `Setting`（齿轮）也不撞。

26 个图标名已逐个对着 `@element-plus/icons-vue@2.3.1` 的真实导出表核过，全部存在
（写错名字会让整个后台白屏，不是小事）。

### 13.2 新增第三道 preflight 关卡

`scripts/check-admin-menu-icons.js`，接进 `npm run preflight:local`：

| 检查 | 说明 |
|---|---|
| 图标重复 | 两个菜单项共用同一图标即报错 |
| 图标名不存在 | 对着已安装的 `@element-plus/icons-vue` 导出表校验（未装依赖时跳过并说明） |
| 多余 import | import 了却没用上的图标名 |

反向验证：把 `分类管理` 改回 `Grid`，报
`图标 Grid 被 2 个菜单项共用：分类管理、功能入口` + `import 了 PriceTag 但 menuItems 里没用到`；还原后转绿。

### 13.3 修复：两处按下反馈方向和全仓相反

写脚本把所有 `.x` / `.x:active` 的背景色解析出来、换算亮度比对了一遍：

```
按下变深 17 处   ← .res-dl / .fav-icon / .chip / .legal-retry / .detail-retry …
按下变浅  2 处   ← packageC/profile/list.wxss 的 .pl-retry 与 .pl-redownload
```

两处都在同一个文件、静息色同为 `--sky-2`，却按到更浅的 `--sky-3`——
在「下载记录 / 学习足迹」页按下去像是弹起来，全 App 独此一家。
改按到 `--sky`（深一档），与静息色相同的 `.res-dl` / `.fav-icon` 完全对齐。
复核：19 处全部变深，0 处变浅。

### 13.4 已核对、无需改动

| 项 | 结论 |
|----|------|
| 活动详情 / 报名页新增的错误态与「重新加载」 | 层级互斥（loading → loadError → notFound → detail）无重叠；底部操作栏加了 `wx:if` 不在错误态露出，对 |
| `.detail-refresh-bar` 用 24rpx、`.pl-refresh-bar` 用 28rpx | 不是不一致：活动详情的主卡是 `margin: -40rpx 24rpx`、底栏 `padding: 16rpx 24rpx`，本页栏距就是 24rpx；个人中心列表是 28rpx。各自对齐了自己的页面 |
| 报名表单字段级报错 `.field-err` / `.field-error` | `#C0392B` 与全仓错误红一致 |
| `.pl-retry` 放在 `.empty-state` 里 | `.empty-state` 是 `flex column + align-items:center`，胶囊自动内容宽居中，与「隐私协议」页的 `.legal-retry` 同一套写法 |
| 反馈页附图标签加「需登录」 | 长度增加 4 字，`.fb-label` 无固定宽、自由折行，排版无影响 |

---

---

## 14. UI 视觉第六轮（V1.6 · uibatch6 错误态对接）

对接 `docs/uibatch6-ui-handoff.md`。工程侧已铺好状态机与 `.page-state-*` 占位 class
（**intentionally 朴素**），本轮做视觉 polish，**未改任何 `wx:if` 分支顺序、状态字段名与 `onRetry` / `onBackList` 绑定**。

### 14.1 `.page-state-*` / `.page-refresh-bar` 上妆

| Class | 改前 | 改后 |
|---|---|---|
| `.page-state-action` | 只有一行 navy 文字 | 胶囊：`--sky-2` 底、26rpx/600、`16rpx 40rpx` 内边距、28rpx 圆角，按下 `--sky` |
| `.page-refresh-bar` | 只有一行红字 | 红底红框条：`rgba(192,57,43,.08)` + `.18` 描边 + 16rpx 圆角，按下 `.12` |
| `.page-state-link` | 已就位 | 补 `:active { opacity: .7 }` |
| `.page-state-box` / `.page-state-text` | 已就位 | 不动 |

配方直接取自活动详情/报名页既有的 `.detail-retry` / `.detail-refresh-bar`，
两边只是「页面内私有 class」与「全局 class」之别。

### 14.2 刷新条横向边距按各页栏距逐页核对

全局默认 28rpx。逐页量过各自内容栏距后发现**一半页面对不上**——
不处理的话红条会比下方卡片各窄 4rpx，边缘错位：

| 栏距 | 页面 | 处理 |
|---|---|---|
| 28rpx | 资源列表 `.res`、动态/展馆/文创/课程详情 `.detail-wrap` | 用全局默认 |
| 32rpx | 文创列表、活动/展馆/动态/课程 Tab 页 | 各自 wxss 覆写 `margin-left/right: 32rpx` |

覆写带页面根类（`.news-page .page-refresh-bar` 等）提权，不依赖 app.wxss 与页面 wxss 的先后顺序。
另写脚本把 10 个页面「生效边距 vs 本页栏距」逐条比对过，10/10 对齐。

### 14.3 四处「重新加载」胶囊统一到同一配方

此前各写各的：`.detail-retry` / `.enroll-retry` 用硬编码 `#EAF1FB` → `#E1ECF9`，
`.pl-retry` 用 `--sky-2` → `--sky`。静息色差 2 个色阶（看不出），
但**按下深度差一倍以上**（Δ亮度 7 vs 18）——同一个胶囊在不同页面按下去手感不一样。
四处（`.page-state-action` / `.detail-retry` / `.enroll-retry` / `.pl-retry`）
统一为 `--sky-2` → `--sky`。`.legal-retry` 是白底描边的另一种形态，未并入。

### 14.4 错误态补图标

同页的空态（`.empty-state`）都有 40px / `#C3CCE0` 的模块图标，错误态原本只有一行灰字，
两个全页状态看着不像一家人。10 处「加载失败」前补 `<icon name="alert" size="40" color="#C3CCE0">`。

- 新增 `alert` 图标（圆角三角 + 感叹号，1.8 描边），全仓仅此一处三角形，不与既有 45 个撞形；
  已实际渲染出图核对过笔画粗细与圆角与整套一致。
- 颜色沿用空态的 `#C3CCE0` 而非错误红：警示语义由三角形与文案表达，
  红色留给正文中间那条必须抢注意力的 `.page-refresh-bar`。
- 「不存在或已下架」的 4 处 notFound **有意不加**——那是内容状态不是故障，
  挂警示三角会过度报警。

### 14.5 新增第四道 preflight 关卡

按 handoff 第 3 条落地 `scripts/check-press-feedback.js`：同一 class 既有静息背景又有
`:active` 背景时，`:active` 必须更深。相比 uibatch5 的手工扫描，这版能解析 `rgba()`
（按叠在 `--paper` 上合成后再比，所以同色加大 alpha 也算变深），因此覆盖到了刷新条这类半透明面。
当前 23 处可点面全部合规。

反向验证：把 `.page-state-action:active` 与 `.pl-retry:active` 改成 `--sky-3`（变浅）→ 两条都报红；
把 `.page-refresh-bar:active` 的 alpha 从 `.12` 调到 `.03` → 报红（证明 rgba 分支有效）；还原后转绿。

### 14.6 handoff 验收勾选

- [x] 全局 `.page-state-*` 与活动详情 retry 风格一致（§14.1、§14.3）
- [x] 刷新失败条在各页边距与主内容对齐（§14.2，10/10 核对）
- [x] 骨架屏与错误态互斥 —— 工程侧已用 `!error` 条件，10 个页面逐个确认过
- [x] 错误态下无漏网可点按钮 —— 错误/notFound 分支里只有 `onRetry` / `onBackList`，
      详情正文与底部操作栏都在 `wx:elif="{{content}}"` 内，错误态下整块不渲染
- [ ] 弱网首次进入 → 加载失败 → 重试 → 正常内容（需真机/模拟器弱网，代码侧无法验证）
- [ ] 有内容时静默刷新失败 → 保留旧列表/详情 + 顶栏可点重试（同上）

> 后两条是运行时链路，需在开发者工具「弱网模拟」下走一遍。

---

---

## 15. 公司主体备案路径下的复审（V1.7）

> **前提变更**：备案主体从**校方**改为**公司**。这一条改变了本文档 §2 的立论基础——
> §2 把校名、校徽、书法字、机构全称、版权行列为「不可改 · 学校 VI」，
> 那是在「小程序属于学校」的假设下成立的。主体换成公司后，同一批资产变成了**风险项**。
> 以下按「会被驳回 / 可能被问 / 低」分级，全部不缩减功能。

### 15.1 先纠正一个认知：Mock 数据不影响审核，兜底 DEFAULT 才影响

排查时发现两类「假数据」的生命周期完全不同，之前 §3.11 把它们混为一谈了：

| 来源 | prod 行为 | 审核员是否看得到 |
|---|---|---|
| `mock/defaults.js`（文章标题、课程名、分类项） | `useMock:false` → `withListFallback` / `mockOrEmpty` 一律返回空 | **看不到** |
| `utils/miniappConfig.js` 的 `DEFAULT`（搜索热词、AI 示例问题、欢迎语） | 后台没配就用它，**与环境无关** | **看得到** |
| `utils/legalDocuments.js` 的 `BASELINE`（协议正文） | 同上 | **看得到** |
| 写死在 WXML 的文案 | 恒定 | **看得到** |

所以「阳明心学十二讲」「长征精神与红色交通史」这些 Mock 条目**不必改**（改了也进不了包），
而搜索热词里的「红色交通」「非遗银饰」是**真的会显示出来**的。本轮只动后三类。

### 15.2 【会被驳回】主体与展示不符 —— 需你们决策，我未改

小程序注册主体是公司，但界面从头到尾自称是**贵州交通职业大学中华文化书院**：

| 位置 | 当前内容 | 性质 |
|---|---|---|
| 首页 Header | `home-title-calligraphy.png`（书法校名） | 学校 VI 图片 |
| 登录页 | `贵州交通职业大学 · 中华文化书院` + 英文校名 | 首屏，审核员第一眼 |
| 关于页 | `ab-org` 机构全称 | |
| **关于页 / 隐私页页脚** | `© 2026 贵州交通职业大学中华文化书院` | **版权归属声明** |
| 个人中心 | 未设置学院时兜底显示机构全称 | |
| 分享海报 | 顶栏 `中 华 文 化 书 院` + 校徽 | **会被转发出去的对外物料** |

微信的判定逻辑不是「有没有出现学校名」，而是「**这个小程序看起来是谁在运营**」。
版权行写学校名，等于声明运营方是学校——与注册主体直接冲突，属于典型的
「主体信息与实际提供的服务不符」。海报还会被转发到朋友圈，扩散面最大。

**两条路，任选其一，都不影响功能：**

1. **拿授权书**（推荐）：请校方出一份《授权委托书》，写明授权贵司以本校名义运营该小程序、
   使用校名与校徽。提审时作为补充材料上传。这样界面一个字都不用改。
2. **改归属表述**：版权行改成 `© 2026 <公司全称>`，机构名降级为内容来源——
   例如关于页写「内容由贵州交通职业大学中华文化书院提供」。
   校徽/书法字建议同步撤下或替换为平台自有标识。

> 这一条我没有动代码：既需要公司全称（甲方资料），也涉及 §2 的既定原则，
> 属于你们的商务决策，不该由我替你们定。**但它是本次复审里唯一可能直接导致驳回的一条。**

### 15.3 【会被驳回】思想政治教育定位 —— 已修

这是本轮最要紧的**已修**项，而且带一点讽刺：§12 那轮为了规避「文化」，
把关于页 slogan 从「传承中华优秀传统文化 · 培根铸魂启智润心」
改成了「"马院 + 书院"协同育人 · 云端思政平台」——
**用一个更敏感的时政表述换掉了一个较温和的文化表述**。

「思政平台」= 思想政治教育平台，「马院」= 马克思主义学院。
公司主体运营涉及时政/党建/思政教育的内容，属于需要相应资质的范畴，
而且这行字就印在**登录页首屏**上。

| 位置 | 改前 | 改后 |
|---|---|---|
| `pages/login/index.wxml` `foot-b` | “马院 + 书院”协同育人 · 云端思政平台 | 校园学习与活动服务平台 |
| `pages/login/index.wxml` `foot-s` | 云端学习平台 · 线上线下相结合 | 线上线下相结合 · 随时随地学习 |
| `packageC/about/index.wxml` `ab-slogan` | 同 `foot-b` | 校园学习与活动服务平台 |
| `packageC/about/index.js` `intro` | 云端书院是“马院 + 书院”协同育人的线上思政平台，依托中华文化书院资源，整合… | 云端书院是面向校园的线上学习服务平台，整合… |
| `pages/index/index.wxml` 区块标题 | 协同育人 | 关联应用 |

功能一个没少：关于页还是那五个功能卡，首页那一栏还是关联小程序入口。

### 15.4 【可能被问】学籍身份采集 —— 已改措辞

登录页写「首次微信登录须绑定**校方导入的学号账号**」，隐私政策写
「账号与**学籍信息（学号、姓名、学院等）**」。公司主体采集在校学生学籍信息，
审核员大概率会追问数据来源与授权依据。

| 位置 | 改前 | 改后 |
|---|---|---|
| 登录按钮 | 绑定学号并登录 | 绑定账号并登录 |
| 绑定提示 | 首次微信登录须绑定校方导入的学号账号 | 首次微信登录须绑定管理员分配的账号 |
| 隐私基线 | 账号与学籍信息（学号、姓名、学院等） | 账号信息（登录账号、姓名、所属单位等） |

后端字段名 `studentNo` 未动（不可见，改动面大且无收益）。
**注意这只是措辞**——如果实际确实要导入全校学生名单，仍需在隐私政策里如实写明来源，
并由校方出具数据提供说明。

### 15.5 【可能被问】后台没配内容 = 「功能不完整」 —— 需你们准备

因为 prod 关掉了 mock，**提审时如果后台还没录内容，审核员打开就是一片空态**。
微信对「内容不完整、无法正常体验」是明确的驳回理由，这比留着旧文案更危险。

提审前后台至少要有：每个 Tab 3–5 条真实数据、1–2 个可进的展馆、1 门可播的课程、
1 个可报名的活动、协议正文（否则走 §9.4 的内置基线，页面会显示「当前为内置基线版本」的提示条）。

同时把兜底 `DEFAULT` 里的内容话题也清了（这份在 prod 会显示）：

| 项 | 改前 | 改后 |
|---|---|---|
| `searchHotTags` | 阳明文化 / 屯堡地戏 / 红色交通 / 非遗银饰 / 知行合一 | 线上展馆 / 精品课程 / 活动报名 / 学习资源 / 文创展示 |
| `aiAssistantChips` | 什么是阳明文化？/ 屯堡文化有何特色？/ 龙场悟道讲了什么？ | 平台有哪些线上展馆？/ 怎么报名参加活动？/ 在哪查看学习足迹？ |
| `aiAssistantWelcome` | …基于书院知识库…学习相关问题 | …基于平台知识库…使用与学习相关的问题 |

顺带这些热词/示例问题指向功能而非内容，对新用户其实更有用。

### 15.6 【需你们确认】AI 标识要与实际实现一致

§9.1 我加了「内容由 AI 生成，仅供参考」。现在要提醒一个反向风险：
**这行字等于主动声明自己是生成式 AI 服务**，而生成式 AI 服务对公众开放需完成算法/大模型备案。

`docs/运维/东风就绪清单.md` 写的是「AI 问答 ✅ 关键词 MVP，可选智谱 Key（非阻塞）」，
也就是说上线形态有两种，标识文案应当跟着走：

| 上线形态 | 建议标识 | 备案 |
|---|---|---|
| 只用关键词 / 知识库检索（不接大模型） | 「答案来自平台知识库，仅供参考」 | 不属于生成式服务，无需备案 |
| 接入智谱等大模型 | 保留「内容由 AI 生成，仅供参考」 | **需完成生成式 AI 服务备案**，且隐私政策要写明第三方模型调用 |

代码我没动——这取决于你们上线时接不接 Key。**定了告诉我，我改一行就好。**

### 15.7 【低】其余观察，均未改

| 项 | 判断 |
|---|---|
| 路由仍是 `pages/news/*`、`packageA/news/*`，API 也是 `/news` | 页面路径在提审时可见，但审核看的是界面呈现，路由是英文标识符，实际很少因此被问。改动面覆盖全部跳转、分包配置与后台配置，收益不匹配，建议不动 |
| 「书院」「课程」「课时」「学习进度」 | 教育信息服务类目下正常，不涉及学科培训，无需办学许可 |
| 「文创展示」「精品好物」「合作咨询」 | 已在 §9.3 去掉电商引导，站内无交易环节 |
| 展馆/课程分类里的「红色教育」「思政必修」「主题宣教」「文化艺术」 | 只在 `mock/defaults.js`，prod 不加载（见 §15.1）。但**后台分类管理里若照抄这套，就会真的显示出来**——建议后台配置时同步中性化 |
| Mock 文章/课程标题 | 同上，dev 专用，不进包 |

### 15.8 提审前动作清单

- [ ] **（决策）** 主体归属二选一：拿校方授权书，或改版权行为公司全称 —— §15.2
- [ ] **（决策）** AI 是否接大模型，据此定标识文案与是否备案 —— §15.6
- [ ] **（运营）** 后台录入各模块真实内容，避免空态 —— §15.5
- [ ] **（运营）** 后台协议正文录入，让来源变 remote（否则显示内置基线提示条）
- [ ] **（运营）** 后台分类名同步中性化，别照抄 Mock 里那套 —— §15.7
- [x] 界面时政表述清除 —— §15.3
- [x] 学籍采集措辞中性化 —— §15.4
- [x] prod 兜底 DEFAULT 内容话题清除 —— §15.5

---

---

## 16. 主体归属落地与 demo 中性化（V1.8）

§15 提出的两条待决策项已定案并落地。补充的事实前提：小程序注册名是**通途云**、
主体是**贵州云漫科技有限公司**、备案号 **黔ICP备17005610号-10X**、已完成微信认证。

### 16.1 三个环节，别混为一谈

| 环节 | 谁审 | 审什么 | 状态 |
|---|---|---|---|
| 微信认证 | 腾讯 | 主体身份真实性 | ✅ |
| 小程序备案 | 腾讯代收 → 省通信管理局 | 主体资质 + 名称 + 服务内容概述。**形式审查，不打开小程序** | ✅ |
| **版本审核** | **微信审核员（真人）** | 上传代码后**逐页点开看**，界面/内容/功能是否与类目和主体相符 | ⬜ |

前两关通过推断不出第三关会过：备案是对着材料做形式审查，版本审核是打开成品看。
本文档 §9 起讨论的「审核员」全部指版本审核。

### 16.2 主体归属：按方案 B 落地（授权范围与展示范围对齐）

校级授权走不通（校长出于风险考虑要走会议流程，周期长），改由书院出具授权。
既然授权方是书院，**界面就不能再打学校的旗号** —— 否则授权范围与展示范围对不上，
授权书本身也就失去意义。

| 位置 | 改前 | 改后 |
|---|---|---|
| 关于页 / 隐私页版权行 | © 2026 贵州交通职业大学中华文化书院 | © 2026 贵州云漫科技有限公司 |
| 关于页页脚 | 定位 + 版权 | **四层归属**：定位 → 内容由中华文化书院提供 → 运营主体 → 备案号 |
| 登录页机构行 | 贵州交通职业大学 · 中华文化书院<br>Guizhou Communications Polytechnic University | 中华文化书院<br>Academy of Chinese Culture |
| 登录页 / 关于页 / 海报徽记 | 圆形**校徽**（学校资产） | 篆体「書」**书院印**（书院资产） |
| 个人中心机构兜底 | 贵州交通职业大学 · 中华文化书院 | 中华文化书院 |

首页 Header 的书法字是「中华文化书院」——**书院自己的名字**，保留。
撤下的只有明确写着「贵州交通职业大学」的圆形校徽与英文校名。

> 英文名取自 `design/brand/logo/extracted/academy-full-ink.png`，
> 该素材本身就是「中华文化书院 / Academy of Chinese Culture」的组合标。

### 16.3 篆印替换带出来的三个坑

换素材不是改个路径就完事，校徽是 1:1、篆印是 560×499（1.122）：

1. **配色要分背景**。渲染对比后确认：金印落深蓝（登录页/关于页）清亮，
   而**金印落白底整个发灰**；海报有「鎏金」模板，金印在金底上几乎不可见。
   故深色背景用金印并**去掉白底圆**，白底圆（海报预览与画布）用深蓝印。
   顺带去掉登录页那条 `0 20rpx 56rpx` 向下投影——没有实体白盘再投影，
   只会在深蓝上糊出一块脏暗圆。
2. **画布会把印面压扁**。`ctx.drawImage(badge, x, y, box, box)` 硬画正方形，
   1:1 的校徽看不出问题，1.122 的篆印会明显变形。抽出 `badgeFitRect` 按图片
   **真实比例**做 aspectFit（不硬编码 1.122，换素材不用回来改），补单测并反向验证。
3. **六处容器尺寸都要重算**。海报预览 `.p-seal-img` 118 正方经 aspectFit 后对角
   158rpx，比 150rpx 的圆还大，四角会露到圆外；画布侧有 `ctx.clip()`，
   同样超框会被裁成缺角。统一到 **0.61 占比**（方形内接圆的临界是 0.747），
   预览与成品一致。

顺带修一个真 bug：关于页 `icp` 兜底是空串、`wx:if` 恒假，**这行从未渲染过**——
备案号在小程序里根本看不见，而这是《互联网信息服务管理办法》要求展示的。
拉取逻辑还写着 `icp: res.icp || ''`（其余字段都是 `|| this.data.xxx`），
后台一旦返回但没带 icp，兜底值会被抹成空。两处一并改掉。

包体积：移除已无引用的 `school-badge.png` 与 `school-title.png`，新增两枚篆印 68KB，
**主包净减 242KB**。

### 16.4 管理后台（不审核，但要改）

微信审核员访问不到后台，改它不影响过审。改的理由是：内部系统文案在验收、检查、
纠纷时都是书面证据，「云端思政平台」白纸黑字写在登录页等于书面确认了这个定位。

- 登录页机构行与涉政表述同步小程序端。核算后发现 `.brand-side` 是 max-width 860px
  的 46%、减 padding 只剩 316px，而中英文合成一行正好需要 316px——**零余量、必然折行
  且断点不可控**（可能断在 Academy 中间），故拆成两行。
- **默认凭据 `admin / Admin@123` 原本无条件渲染**，生产环境任何人打开后台登录页
  都能看到。加 `v-if="isDev"`，生产构建下 Vite 会整段摇掉。
- 「新闻」→「动态」共 19 处（菜单、标题、按钮、占位符、权限分组、分类模块名、
  Banner 链接类型、封面说明）。只改中文标签，`news` 路由与权限 key 一律不动。
- 「协同育人」→「关联应用」：不只是避词——小程序端该区块标题已改，
  后台描述必须同步，否则运营在后台看到的名字在小程序里找不到对应位置。

### 16.5 demo 内容中性化（理由不是过审）

`mock/defaults.js` 在 prod 下 `useMock:false`、一律返回空，**审核员看不到**。
改它是因为：虚构活动挂了真实地标（「红色交通史主题研学行 · 遵义会议会址」）、
给甲方演示走的是 dev 环境、以及**运营配后台时最容易照抄这一套分类**。

**保留不动**：`hallsHome` / `hallsFull` / `hallDetail` 是真实展馆带真实 720 云 VR 链接，
`colleges` 是真实学院名。改真实名称等于信息不实——用户点进 VR 看到的实景会和名字
对不上，这比用词敏感严重得多。分类「红色教育」「博物馆与校史」如实描述了场馆性质，
模糊化反而不准确。

改写的部分保持**具体、能撑起界面**（dev 下还要靠它演示，不能退化成「示例一二三」），
但主题中性、不挂真实地标与真实人物。另外「主题宣教」→「主题展馆」
（宣教即宣传教育，政治色彩重，而归在这类的是节水宣传教育中心）。

校验：12 个数组条数一个没变、页面引用的 18 个 mock 键全部存在、VR 链接与各 detail
块字段结构完整。

### 16.6 AI：首版就带上，如实标注

§15.6 曾建议「先弱化 AI、过审后再改回来」。**该建议已撤回**，两个原因：
AI 是产品卖点，藏起来与「让校方看到效果」的上线目的冲突；更关键的是
**小程序每次发版都要重新过审**，第二版加回显著 AI 功能照样会被看到——
风险没消失，只是推迟了，还多花一轮周期。

标识文案的原则也纠正过来：**必须如实反映实现，不能为了过审去挑文案**。
既然确定接智谱，就保留「内容由 AI 生成，仅供参考」。

备案判断：登录封闭性由代码保证（强制登录 + 无自助注册 + 微信登录必须绑定
管理员导入的账号），属于《生成式人工智能服务管理暂行办法》第二条的
「未向境内公众提供」，不适用该办法；底层模型由智谱已完成备案。
**建议仍向贵州省网信办咨询一次**（免费、常规），拿到口径更踏实。

代码侧同步做了两件事：
- **提示词冒了机构名义**：`SYSTEM_PROMPT` 原写「你是贵州交通职业大学中华文化书院的
  智能文化助手」。提示词决定模型如何自称，而模型的回答用户直接看得到——它会照着
  这个身份自我介绍，与刚理顺的主体归属矛盾。改为只说平台名。
- **上游失败没有降级**：`canUse()` 只看有没有配 Key，不看上游是否可用；接的是免费
  模型，QPS/额度超限是常态。改成失败与返回空都降级到知识库作答。
  新增 5 例单测（含「提示词不得含机构名」），反向验证后全量后端 364 测试通过。

### 16.7 仍需你们在微信后台操作（不是代码）

- [ ] **服务类目**：现为「资讯 > 信息资讯」，与主功能不符——七个模块只有「动态」
      属于资讯。这是本次复审里最直接的驳回风险。后台显示本月还可改 5 次
- [ ] **小程序介绍**：现为「数字化平台整合优质育人资源，构建线上线下协同育人新格局！」，
      与资讯类目不符且带教育属性，改成功能陈述
- [ ] **小程序简称**：尚未设置。设成「云端书院」可在微信侧建立「通途云=云端书院」的
      关联，界面显示产品名就不突兀了
- [ ] **后台录入真实内容**：prod 关了 mock，空态同样按「功能不完整」驳回
- [ ] **后台分类名**：别照抄 mock 那套
- [ ] **书院授权书**：授权范围写「书院名称与书院标识」，与界面展示范围一致

---

---

## 17. 印章重做与两处事实更正（V1.9）

### 17.1 上一轮的错误：只换素材，没做设计

§16.3 把圆形校徽换成了方形篆印，**却保留了圆形容器**——方印塞进 300rpx 的圆里，
四角空荡、两种形状互相打架。素材替换不等于设计。

重做为管理后台登录页那套语言：**圆角方框 + 金边 + 内发光**，形状与印面呼应。
参数不照搬后台的 68px，而是渲染比对后定的：

| 参数 | 后台 | 登录页 | 依据 |
|---|---|---|---|
| 容器 | 68px | **248rpx** | 方形视觉重量约为同尺寸圆的 1.27 倍，等尺寸换过来会压住下面的「云端书院」，故由 300rpx 收到 248rpx |
| 印面占比 | 0.647 | **0.70** | 后台在 68px 小尺寸下 0.647 紧凑；放大四倍后同比例显空。渲染对比 0.645 / 0.70 / 0.74，0.70 最饱满，0.74 已贴内框 |
| 圆角比 | 0.235 | **0.226** | 与印面本身的圆角呼应 |
| 金边 | 2px `#C9A227` | 2rpx 同色 | 比印面的暖金 `#E8D4A0` 暗一档，形成「暗金框 + 亮金印」层次 |
| 外框 | 无 | **外扩 22rpx 的细金线** | 延续旧版双环的层次语言，只是跟着改成圆角方形 |
| 内部 | 6% 白 | **165deg 顶光渐变** | 与页面 170deg 的深蓝渐变同向，光感才连得上 |
| 发光 | 30/20px | 60/40rpx | 按尺寸放大，否则大尺寸下显干瘪 |

六处徽记（登录页 / 关于页 / 海报预览两处 / 画布两处）**参数完全一致**：
占比 0.70、圆角比 0.226。

海报侧同样处理：白色**圆**牌配方印一样不对，改圆角方牌；画布的 `drawBadgeCircle`
从 `ctx.arc()` 改为 `roundRectPath()` 并更名 `drawBadgePlate`，
`badgeFitRect` 的方框系数随之由 0.61 调到 0.70（方牌不再受「内接圆」约束）。

### 17.2 事实更正一：学院也是 demo

§16.5 曾把 `colleges` 的 11 个学院名当成真实数据保留——**这是错的**，
经确认只有展馆是真实的，学院全部是演示内容。挂着真实高校的院系名做虚构数据，
和「遵义会议会址」是同一类问题。

### 17.3 事实更正二：关联小程序只有「通途星」

现实中只有一个关联小程序「通途星」，AppID 尚未拿到。
mock 相应调整为：`collegesHome` 只放通途星一条（首页横滑就该反映现实），
`colleges` 放通途星 + 两条**明确标注为示例**的条目撑起列表布局。

> AppID 到位后配置在 `miniapp/config/navigate-appids.json`，
> 由 `scripts/sync-navigate-appids.js` 同步进 `app.json` 的跳转白名单。

---

---

## 18. 实机截图带出的三处修复（V1.10）

### 18.1 个人中心仍显示学校名 —— 兜底改了但没用上

`{{userInfo.college || '中华文化书院'}}` **只在字段为空时才兜底**。而 `profile.college`
是后端返回的真实值，历史数据里存的就是「贵州交通职业大学 · 中华文化书院」这串
界面占位文案——有值就原样显示，所以主体归属改完后个人中心照旧打着学校的名号。

改为按占位串名单归一化（复用 §16.2 建的 `PLACEHOLDER_COLLEGES`）：真实学院名照显，
占位串（含改文案前的旧串）一律显示「中华文化书院」。**不指望回头去洗历史数据。**

### 18.2 海报封面加载失败：预览留空白框，成品却是徽记布局

画布侧 `coverImg.onerror` 早就会降级成「居中徽记」布局，而预览侧只看 URL 合不合法、
不看图片是否真的加载得出来——于是预览是一个空白灰框、存下来的图却是徽记布局。
又一次「预览 ≠ 成品」。给 `.p-cover-img` 加 `binderror`，失败时把 `hasCover` 置 false，
两侧行为对齐。

### 18.3 登录页留白过大 —— 根因不是缺装饰

`.login-form` 是 `margin-top: auto`，**剩余空间全堆在品牌区与表单之间**；印章由
300rpx 收到 248rpx 后，多出来的 52rpx 又全落进那一块，于是显得空。

渲染比对了四种处理（现状 / 空白上下均分 / 放大印章到 272 / 加一道装饰细纹），
结论是**空间再分配**而不是填东西：
- 加细纹与上方既有的 ❖ 分隔纹重复，画蛇添足；
- 印章放大到 272rpx 会压住「云端书院」标题，呼吸感变差。

做法：`.seal` 的 `margin-top` 由 48rpx 改为 `auto`，与 `.login-form` 的 auto 配对，
剩余空间上下均分；48rpx 并进 `.login-inner` 的 `padding-top`（60 → 108rpx）作为保底，
**空间不足时印章距顶与改前逐像素一致**（108+statusBar ≡ 60+statusBar+48），
小屏退回紧凑布局，不会贴住状态栏。

各机型分配（rpx）：iPhone 8 剩余为负 → 两处 auto 归零；iPhone 13 上下各 +54；
15 Pro Max 上下各 +142。

### 18.4 活动列表页顶部标题截断 —— 未修，需真机复验

`pages/activity/index.json` 只有 `navigationBarTitleText`，用的是**微信原生导航栏**；
而截图里正常的个人中心、生成海报都是 `navigationStyle: custom` 自绘的。
原生导航栏由微信绘制，页面 CSS 影响不到它，**代码侧找不到能造成截断的因素**。

建议：真机复验一次。若真机正常，是开发者工具模拟器的渲染问题；若真机也截断，
可把该页改成与站内其他二级页一致的自绘 `nav-bar` 绕开。

> 另记一处产品命名：这是活动**列表**页，标题却是「活动报名」，用户可能以为已进入
> 报名流程。与首页入口「报名」是一致的，故未擅自改，供你们决定。

---

---

## 19. 数据层复审：默认文案与 demo 内容（V1.11）

前十八节改的都是**代码**。这一轮把范围往下推了一层，结论是：
**真正显示给审核员看的，很多不在代码里，而在数据库里。**

### 19.1 后端默认文案一直在盖掉前端已改好的兜底 ⚠️ 本轮最严重的一处

`SysConfigService` 里写死了一批「后台没配时返回什么」的默认值。这不是示例数据——
接口返回的就是它们，小程序照单展示。而它们全都停留在改主体之前的版本：

| 配置项 | 旧默认值 | 影响 |
|--------|----------|------|
| `about_intro` | 「云端书院是"马院 + 书院"协同育人的线上**思政**平台……阳明文化、屯堡文化、红色文化与非遗技艺……」 | 关于页正文。就是 §12 从登录页撤下来的那句 |
| `doc_privacy` | 开头即「**贵州交通职业大学**中华文化书院（我们）」，落款留 `shuyuan@gzjtzy.edu.cn` | 隐私政策全文 |
| `doc_agreement` | 「遵守本协议及**学校**相关规定……AI **文化**助手」 | 用户协议全文 |
| `ai_assistant_welcome` | 「我是书院**文化**助手，解答**文化**相关问题」 | 问答页首屏 |
| `ai_assistant_chips` | 阳明文化 / 屯堡文化 / 龙场悟道 | 问答页推荐问题 |
| `search_hot_tags` | 阳明文化 / 屯堡地戏 / 红色交通 / 非遗银饰 / 知行合一 | 搜索页热词 |
| `contact_phone` / `contact_email` | `0851-12345678` / `shuyuan@gzjtzy.edu.cn` | 关于页联系方式 |

关键在于**优先级**：小程序侧写的是 `res.intro || this.data.intro`，
后端只要返回非空就赢。所以 §16 里改好的 `miniappConfig.js` DEFAULT 与
`legalDocuments.js` BASELINE **一次都没有生效过**。

改法：

- 简介 / 欢迎语 / 推荐问题 / 热词 → 与小程序侧 DEFAULT **逐字对齐**，两边不再有第二个版本；
- 联系地址 / 电话 / 邮箱 → 默认留空。关于页这三行都有 `wx:if`，空值即整行不渲染。
  与其展示编的号码和学校 edu.cn 邮箱，不如不展示；真值请在后台「内容配置」填；
- 隐私政策 / 用户协议 → 后端不再内置正文，返回空串。基线正文**只留一份**，
  在 `miniapp/utils/legalDocuments.js`，小程序会自动落到那份（`resolveFromSources`）。
  后台编辑器显示空白，正好提示这两份文档尚未正式配置。

新增 4 条单测钉住这个约定（`SysConfigServiceTest`），其中一条按黑名单扫描全部默认文案，
出现「贵州交通职业大学 / gzjtzy.edu.cn / 马院 / 思政 / 阳明 / 屯堡 / 红色 / 非遗 / 文化助手」即失败。
反向验证过：把欢迎语改回旧值，两条断言同时红。

### 19.2 个人中心显示学校名的**源头**在后端

§18.1 在小程序侧做了占位串归一化，但字符串是后端造的：
`ProfileService#toMemberVo` 在 `college` 为空时回填「贵州交通职业大学 · 中华文化书院」。
源头不改，其它调用方（后台、报名导出）仍会拿到它。已改为「中华文化书院」；
旧串已落在历史数据里，`profileForm.js` 的 `PLACEHOLDER_COLLEGES` 继续认它。

### 19.3 首页「关联应用」显示的三个学院

截图里的马克思主义学院 / 轨道交通学院 / 智能交通学院来自 `seed-dev.sql` 的
`college_app` 表——铺了 11 个学院名。按 §17.2 确认的事实，**真实关联对象只有「通途星」一个**，
且 AppID 尚未拿到。已收敛为 通途星 + 2 条明确标注的示例条目，
`navigate-appids.template.json` 同步更正。

### 19.4 demo 内容整体中性化

`seed-dev.sql` 里除展馆外的全部条目重写为中性、功能导向的示例，并统一加「示例」前缀，
避免被误当成正式内容：动态 6 条、课程 5 门、活动 4 场、文创 3 件、资源 4 份、
分类名、Banner、搜索索引、AI 知识库 5 段。分类名同时与
`miniapp/mock/defaults.js` 的 `categories` 对齐（此前 news / course 两组就对不上，
dev 用 mock 和用真接口看到的筛选条不一样）。

**展馆保持真实，馆名一字未动**——包括「贵州交通红色教育基地」。
真实场馆的名称是事实，改了反而是错的；改的只有我们自己写的介绍文案：
原先是「弘扬长征精神与交通红色基因，开展沉浸式红色文化育人」这类宣传语，
现在改为「基地实体展陈的线上导览入口，支持 VR 全景漫游与图文介绍」这类功能陈述。
`patch-hall-real-data.sql` 同步。

### 19.5 会进生产库的种子

`sql/init.sql` 在**所有**环境执行，它 INSERT 的 `sys_config` 与 `badge` 同样带旧文案
（含两个「文化探索者 / 文化传播者」徽章名，个人中心可见）。已一并更新；
`patch-sys-config-miniapp.sql` 同步。

### 19.6 旧库怎么办：`patch-subject-neutral-config.sql`（新增）

以上改的是代码与建库脚本，**已经建好的库不会自动变**——接口读的是库。
新增这支补丁负责补齐：AI 三项配置、两个徽章名、`member_profile.college` 与
`enroll.college` 的旧占位串。

所有 UPDATE 都带 `WHERE 旧值 = '…'` 精确匹配，**只覆盖从未被人改过的默认值**，
管理员自己写过的内容一律不动，可重复执行。已登记进 `sql/README.md` 第 16 条。

### 19.7 后台侧的提示

「内容配置」页给简介 / 地址 / 电话 / 邮箱补了 placeholder（说明留空的后果），
协议区加了一句说明：留空时小程序显示内置基线，填写后以后台为准。

### 19.8 提示词里的机构名义（补 §16 的遗漏）

§16 处理了 `AiClientService`，但 `AdminAiPolishService` 的系统提示词仍写着
「你是贵州交通职业大学中华文化书院的内容编辑助手」。它产出的稿件会被编辑贴进正式内容，
让模型替一个它并不代表的主体说话是不合适的。已改为只描述文风、不给身份。
`AiCopyAssistFallbackService` 的标题模板「文化传承视角下的…」与 `OpenApiConfig`
的接口描述一并处理。

### 19.9 回答：后台填的 demo 内容要不要改

**要，而且比代码更紧要。** 分三类：

1. **内容运营（动态 / 课程 / 资源 / 活动 / 文创）** —— 审核员点进每个 Tab 看到的就是这些。
   代码里一个字都管不着。提审前必须换成真实内容；暂时没有真实内容的模块，
   宁可下架该入口，也不要留着「屯堡地戏」「长征精神」这类条目。
2. **内容配置 → 关于页 / 协议** —— 本轮已把后端默认值改干净，
   但**如果你们曾在后台点过保存**，库里就有了带学校名的那一版，代码改不到，
   得到后台自己改。自查 SQL 写在 `patch-subject-neutral-config.sql` 末尾。
3. **展馆** —— 真实，不用动。

### 19.10 顺带记一处

`pages/news/index`、`pages/course/index`、`pages/hall/index` 三个 Tab 页
声明了 `enablePullDownRefresh: true` 也写了 `onPullDownRefresh`，
但页面是 `height: 100vh` + 内层 `scroll-view`，页面本身不滚动，
原生下拉刷新**永远不会触发**，那三个回调是死代码。不影响使用（列表有自己的刷新入口），
本轮未动，记在这里备查。

---

## 20. 导航栏方案统一（V1.11）

§18.4 当时的结论是「原生导航栏，页面 CSS 干预不了，建议真机复验」。真机确认后本轮做了迁移。

### 20.1 机理更正

原生导航栏并不会无条件裁切标题——若如此，所有小程序都会出这个问题。
真正的触发条件是**微信的字体大小设置**（我 → 设置 → 通用 → 字体大小）：
原生导航栏标题字号跟随该设置缩放，而栏高固定，调大后下缘就会被切。
自绘 `nav-bar` 走 rpx，不受该设置影响——这也解释了为什么只有这一类页面出问题。

顺带更正一处事实：仓库里用原生导航的**不止一页**，共 5 页
（`pages/activity/index`、`packageA/news/list`、`packageA/hall/detail`、
`packageB/course/player`、`packageC/college/webview`）。

### 20.2 迁移范围

| 页面 | 处理 | 说明 |
|------|------|------|
| `pages/activity/index` | ✅ 改自绘 | 与 `packageC/activity/detail` 同一流程，页面结构（`min-height:100vh`）完全一致 |
| `packageA/news/list` | ✅ 改自绘 | 同上 |
| `packageA/hall/detail` | ✅ 改自绘 | `height:100vh` + `flex:1` 滚动区，与 craft/resource/college 三个列表页同型 |
| `packageB/course/player` | ❌ 保留原生 | `<video>` 是原生组件且要全屏，固定顶栏会和它抢层级 |
| `packageC/college/webview` | ❌ 保留原生 | 根节点是 `<web-view>`，原生组件层级永远压在自绘顶栏之上 |

### 20.3 新增关卡

`scripts/check-wxml.js` 加检查 E：逐页比对 json 的 `navigationStyle` 与 wxml 是否画了顶栏。
用原生导航的页面必须写进 `NATIVE_NAV_ALLOWED` 并注明原因，否则报错。
反向验证过：把 `pages/activity/index.json` 改回原生，关卡立刻拦下。

---

## 21. 登录页留白再调（V1.11）

§18.5 用两个 `margin-top: auto` 把剩余空间对半分。反馈是「不太明显」——复核属实：
`auto` 只会**均分**，中间那段仍有 70~130rpx（随屏高涨），整屏仍被切成上下两半。

改成三根弹性隔条按 **3 : 1 : 3** 分配（印章上方 / 品牌与表单之间 / 表单与页脚之间）：
中间只拿七分之一，其余落到顶部光晕与底部波浪上，那两处本就是留白区。
`.foot` 同时从 `.login-form` 内部移出，成为 `.login-inner` 的直接子元素——
不然它上面那根隔条不参与 flex 分配。

浏览器实测（三档屏，与真实 WXSS 值一一对应）：

| 屏 | 印章距顶 | 品牌→表单 | 表单→页脚 |
|----|----------|-----------|-----------|
| SE 320×568 | 155 → 155 | 44 → 44 | 40 → 40 |
| iPhone 8 375×667 | 148 → 148 | 44 → 44 | 40 → 40 |
| iPhone 13 390×844 | 244 → 237 | **89 → 57** | 40 → 79 |

小屏隔条全部塌成 0，逐像素与改前一致，零风险；高屏上圈出来的那块空白收窄约 36%。
用户真机的可用空间比模型更大，收窄幅度还会更明显。

装饰仍然没加，理由同 §18.5：页面下方已有一组 ❖ 分隔符，再加一组是重复。
另有一条硬约束——iPhone 8 及以下整页已经**正好填满**，添加任何元素都会把小屏推进滚动。

---

---

## 22. 助手浮层遮挡、海报分隔符、后台内部措辞（V1.12）

### 22.1 书院助手抽屉被 tabBar 切掉底部 ⚠️ 视觉 bug

真机截图里，在带底部导航的页面打开书院助手，输入框与发送键整条被导航栏压住。

原因不是 z-index 写小了：抽屉是 `z-index: 610`，tabbar 是 `500`，数值上抽屉更高。
但**自定义 tabBar 由微信在页面之外单独渲染**，它和页面内容不在同一个层叠上下文里，
页面里给再高的 z-index 也盖不住它。所以只能让路，不能压过去。

改法：抽屉在 tabBar 页面上整体抬高 `108rpx + 安全区`（`.sheet.above-tabbar`）。
组件本身不知道自己被放在哪一页，`attached` 时用 `getCurrentPages()` 的 route
比对 tab 路由清单来判断。

顺带修掉一个会多让一截的坑：抬高后安全区那段已经由 tabbar 自己的 `padding-bottom` 占掉，
`.sheet-foot` 里就不能再算一次 `env(safe-area-inset-bottom)`。

**新增关卡 `check:tabbar-routes`**：tab 页清单同时写在三处
（`app.json` 的 `tabBar.list`、`custom-tab-bar/index.js` 的 `data.list`、
新增的 `utils/tabRoutes.js`），任一处改漏，浮层让位就会算错。
关卡把三份比一遍，并核对让位高度与 `.tabbar` 的实际高度一致。
反向验证过：删掉清单里一条、或把让位高度从 108 改成 100，都会被拦下。

### 22.2 哪些页面需要书院助手

改前是 7 处，且规律说不通：四个 Tab 里只有「首页」「动态」有浮标，
「展馆」「课程」没有，看着像漏做；课程详情页和播放页各有一个，是重复入口。

定下的规则：**Tab 层只在首页留一个总入口；内容详情页各留一个；列表页与播放页不留。**

| 页面 | 结论 | 理由 |
|------|------|------|
| `pages/index/index` | ✅ 保留 | 总入口 |
| `packageA/news/detail` | ✅ 保留 | 有具体内容可问 |
| `packageA/hall/detail` | ✅ 保留 | 同上 |
| `packageA/craft/detail` | ✅ 保留 | 同上 |
| `packageB/course/detail` | ✅ 保留 | 同上 |
| `pages/news/index` | ❌ 移除 | 列表页没有上下文；且四个 Tab 只有它有，规律说不通 |
| `packageB/course/player` | ❌ 移除 | 与课程详情页重复；播放场景不该再飘一个球 |

另有一层考虑：AI 是这次提审的风险点，入口越少越聚焦，
「在内容页可就当前内容提问」也比满屏飘球更好解释。这两处是各一行的改动，要恢复很容易。

### 22.3 海报分隔符

旧版是**一条通长实线 + 一个绝对定位压在正中间的 ❖**，两者直接重叠成一团
（预览与画布两边写法一致，所以「预览即成品」是成立的，只是一起难看）。

改成登录页 `.brand-div` 那套：两段线各自朝外淡出，中间给菱形让出空隙。
尺寸按海报宽 480rpx ÷ 登录页 750rpx = 0.64 等比缩下来（线 92→58、间距 20→14），
字号取 18rpx —— 等比算出来是 14rpx，在海报尺度上几乎看不见。
合计 162rpx，与旧版 160rpx 的占位基本一致，不影响上下留白。

画布侧按同一比例换算（海报 480rpx 对应画布 300px，系数 0.625）：
半宽 50px、❖ 半宽 5.5px、间距 9px，两段各用一个 `createLinearGradient` 做淡出。
已在真实 Canvas 2D 上渲染核对，预览与画布逐项对齐。

### 22.4 后台里写给自己人看的话

对着甲方的界面上出现了合同术语、内部排期和命令行指引：

| 位置 | 旧文案 | 问题 |
|------|--------|------|
| 首页轮播 | 「……（**验收标准**：5 分钟内同步）」 | 验收是我们和甲方之间的约定，不是给使用者看的 |
| 操作日志 | 「……满足**验收**「操作有日志」」 | 同上 |
| 展馆编辑 | 「……（**验收 §2.4**）」 | 内部文档章节号 |
| 关联小程序 | 「「接口同步」为**预留**能力，待**甲方** API 文档到位后对接」 | 当着客户面称其为「甲方」；后面还让用户去复制 json 模板、执行 `node` 脚本 |
| 意见反馈 | 「……（用户端消息通知**二期**接入）」 | 排期术语 |
| 登录页 | 「**开发环境**默认账号 admin / Admin@123 / **生产环境**请使用独立超管账号……」 | 已是 `v-if="isDev"`，打包发布时整行不存在；但第二句属于运维文档，不该印在登录页 |

全部改写为面向使用者的说明。其中两处不只是措辞：

- **关联小程序**：原文让后台使用者自己去跑 `node scripts/sync-navigate-appids.js`。
  改为「目标 AppID 需先加入跳转白名单才能打开，新增请联系技术人员配置」——
  这才是后台使用者该知道的边界。「接口同步」选项同时置灰（`disabled`），
  因为它尚未真正可用，选了会静默失效。
- **意见反馈**：查证后确认 `AdminFeedbackService#reply` 只把回复存进库，
  既不建消息也不推送，小程序端**没有任何地方能看到回复**。
  所以文案不是把「二期」删掉了事，而是如实写成
  「回复内容仅保存在后台备查，用户端不会收到通知；需要答复用户时，请使用其填写的联系方式」。

还有一处更严重的：`CollegeAppService#contentTypeLabel` 里 `api_sync` 的标签是
「接口同步（**预留**）」，而这个字符串**会随接口下发到小程序**，
显示在「关联小程序」条目上——内部路线图措辞一路漏到了审核员眼前。已去掉。

**新增关卡 `check:ui-copy`**：扫 `admin/src` 的 `.vue` `<template>` 与 `miniapp` 的
`.wxml`，只看会渲染出来的文本节点和 label / placeholder 一类属性（注释先剔除），
命中「验收 / 甲方 / 乙方 / 开发环境 / 生产环境 / 预留 / 一期 / 二期 / 本期 /
待实现 / 需求文档 / 技术方案 / 联调 / 压测 / 占位符 / TODO / FIXME / mock」即报错。
反向验证过。

> 覆盖不到的地方：后端 Java 里那些会下发到前端的字符串（本轮那条
> 「接口同步（预留）」就是靠人工扫出来的）。没有可靠办法自动判定哪些字符串会露出到界面，
> 只能在改这类 label 时留意。

---

---

## 23. 宽屏（平板 / PC）适配与海报标题折行（V1.13）

### 23.1 先说结论：目前**没有**针对平板做适配

小程序的尺寸单位 `rpx` 是**按屏宽等比缩放**的（1rpx = 屏宽 ÷ 750）。
所以在平板上，一套手机版式会被整体等比放大——不是「重新排版」，是「放大镜」。
实测（浏览器等比复刻真实 WXSS 数值）：

| 机型 | 1rpx | 输入框高 | 正文字号 |
|------|------|----------|----------|
| iPhone 13 390×844 | 0.52pt | 54pt | 15.6pt |
| iPad 768×1024 | 1.024pt | 106pt | 30.7pt |
| iPad Pro 10.5 834×1112 | 1.112pt | 116pt | 33.4pt |

33pt 的正文在平板上偏大，但**比例是对的、不会错位**。真正的错位来自下面这个 bug。

### 23.2 图标不跟着缩放 ⚠️ 真 bug，已修

`components/icon/index.wxml` 里写的是：

```
style="width:{{size}}px;height:{{size}}px;"
```

**px 不随屏宽缩放。** 于是屏幕越宽，图标相对越小：

| 机型 | 输入框高 | 图标 | 图标 / 输入框 |
|------|----------|------|---------------|
| iPhone 13 | 54.1pt | 18pt | 0.333 |
| iPad | 106.5pt | 18pt | **0.169** |
| iPad Pro 10.5 | 115.6pt | 18pt | **0.156** |

也就是截图里 tabBar 图标、顶部「我的」和铃铛看着缩水了一半以上——
全站 46 个图标共用这一个组件，所以每一页都中招。

改法：渲染成 `size × 2` rpx。`size` 的语义保持不变（「375pt 宽机型上的 pt 数」），
在 375pt 机型上 2rpx 正好 1pt，**与改前逐像素一致**；其它宽度按比例缩放。
改后三档屏的「图标 / 输入框」全部回到 **0.346**。

顺带把 SVG 的标称尺寸放大到 3 倍：图标本身是带 `viewBox` 的矢量、与分辨率无关，
但个别基础库会按标称尺寸先栅格化再缩放，组件现在按 rpx 渲染、宽屏上实际显示尺寸更大，
放大标称值兜底，代价为零。

**新增关卡 `check:rpx-scaling`**：扫所有 `.wxml` 的内联 `style`，出现 px 即报错；
只放行状态栏 / 胶囊几何三个变量（`statusBarHeight` / `navContentHeight` / `capsulePadding`——
这些值来自基础库、单位本来就是 px，换成 rpx 反而错位）。另外单独钉死 icon 组件按 rpx 渲染。
反向验证过：把组件改回 px，关卡立刻拦下。

复查了 `.wxss` 里剩下的 11 处 px，都是合法的（离屏画布尺寸、阴影、1px 发丝线），未动。

### 23.3 登录页按钮在宽屏上被收窄 —— 已加固，但需要你复验

真机 iPad 上，登录页两个按钮明显比同一列的输入框窄一大截，
「微信授权登录」的字被切掉半截（按钮文字是 `nowrap`，基础库自带 `overflow:hidden`）。

页面里写的是 `width: 100%`，**在浏览器里等比复刻复现不出来**——三档屏量出来的按钮宽度
都等于内容列宽（iPad Pro 上 700.6pt）。所以判断是**基础库自己的按钮宽度规则**在宽屏下生效了，
那条规则的选择器我们看不到。

处理：对**独占一行**的四个按钮（`lbtn` / `save-btn` / `fb-submit` / `pe-submit`）
用 `width: 100% !important; max-width: none !important` 压住。这里用 `!important`
是有意的——压的是看不见的基础库规则，而这四个按钮本来就通栏，没有副作用。

⚠️ 特意**没有**包含 `activity/detail` 的 `.btn-primary` / `.btn-outline`：
它们在 `display:flex` 的 `.foot-bar` 里，有「单按钮」和「查看凭证 + 取消报名」两种排法，
钉成 100% 会把那一行挤坏。

由于复现不出来，**这一条属于「按最可能的成因加固」，请在开发者工具里再看一眼是否好了**。

### 23.4 关于「平板 / PC 是否应该换一套版式」

应该，但小程序**没有现成的响应式机制**，得自己搭，成本不小。可行的做法只有一种：

用 WXSS 的 `@media (min-width: 431px)`（431pt = 最宽的手机 iPhone 15 Pro Max 再加 1），
在这个断点之上把关键尺寸**改用 px 重新声明**（px 值 = rpx × 430/750），
效果是「超过手机宽度后不再继续放大，锁定成 430pt 的版式并居中」。

没有在本轮做，原因是这件事**不能只做一页**：

1. 尺寸要逐条重声明。以登录页为例，印章、标题字号字距、分隔线、输入框高/圆角/内距、
   按钮、分隔符、页脚——大约 20 条，而且以后每改一个数就要同步两处；
2. **和图标改动耦合**。图标现在按 rpx 缩放（23.2），一旦某页锁定成 430pt 版式，
   那一页的图标就会相对偏大；而图标组件是全站共用的，让它一起锁定，
   没做适配的页面又会退回「图标偏小」。要么全站一起锁，要么都不锁，**不能只锁一页**；
3. 真正的「平板版式」（一行放更多卡片、双栏详情）是另一件事，那是重新设计，不是改单位。

所以现状是：**全站按比例放大，比例正确、不错位**（23.2 修完之后）。
要不要做真正的宽屏版式，建议等上线后按实际使用情况再定——
这个小程序的用户是校内师生，主力机型是手机。

顺带两条事实：

- `app.json` 里**没有** `resizable`。不写的话，iPad 上小程序按竖屏全宽运行、不可旋转；
  写了才允许横屏与分屏。**在版式没有宽屏适配之前，不建议打开**——横屏会更难看。
- 微信开发者工具设备列表里的 **Windows / Mac 三项是「PC 端小程序运行环境」**，
  不是把手机版拉宽。PC 端还需要在公众平台单独开启「PC 端支持」才会真正可用。
  截图里那几项点击无反应，从代码侧找不到成因（页面事件绑定与手机端完全同一套），
  倾向于是开发者工具 PC 模拟的问题；要确认得在真实 PC 微信里试。

### 23.5 图片资产是否够清晰

- **图标：全部是矢量**。`components/icon/icons.js` 是 SVG 路径库，
  运行时拼成 `data:image/svg+xml` 的 data URI，带 `viewBox="0 0 24 24"`，
  任何分辨率下都是矢量渲染，不存在糊的问题。
- **位图只有 3 张**，都有余量（按最宽的 iPad Pro 12.9、dpr 2 计）：

| 文件 | 原始 | 最大显示 | 需要像素 | 余量 |
|------|------|----------|----------|------|
| `academy-seal-gold.png` | 560×499 | 174rpx ≈ 238pt | 476px | 1.18× |
| `academy-seal-navy.png` | 560×499 | 105rpx ≈ 143pt | 287px | 1.95× |
| `home-title-calligraphy.png` | 1300×239 | 高 52rpx，宽 ≈ 386pt | 772px | 1.68× |

结论：现有素材在平板上不会糊。

### 23.6 海报标题折行（图 4）

「贵州交通博物馆 · 教育馆」被折成「……教育」+「馆」，末行一个孤字。
成因是**贪心折行**：一个字一个字往行里塞，塞不下就换行，最后剩多少算多少。

改成「先定行数，再把行数压到最窄」（`utils/posterCover.js` 的 `balanceLines`）：

1. 用整幅可用宽度贪心折一遍，得到最少需要几行 n；
2. 二分找出「仍然只折成 n 行」的最小宽度 W；
3. 按 W 折出来的行自然就均匀了——想让某行更短，就必须多出一行。

目标是**最宽的一行尽量窄**（min-max），不是「每行字数相同」——
25 个字折 3 行的最优解就是 9/9/7，压到 8 会变成 4 行。

真实 Canvas 2D 测量下的效果：

| 标题 | 贪心（改前） | 均衡（本次） |
|------|--------------|--------------|
| 贵州交通博物馆 · 教育馆 | 贵州交通博物馆 · 教育 / **馆** | 贵州交通博 / 物馆 · 教育馆 |
| 西部山区道路运输安全警示教育基地 | 西部山区道路运输安全 / 警示教育基地 | 西部山区道路运输 / 安全警示教育基地 |
| 示例文创：银饰 · 蝴蝶冠 | 示例文创：银饰 · 蝴蝶 / **冠** | 示例文创： / 银饰 · 蝴蝶冠 |
| 校史馆 | 校史馆 | 校史馆（一行放得下就不折） |

**预览与画布共用同一份折行结果**：海报预览是 400rpx 宽配 40rpx 字，
画布是 220px 宽配 22px 字，两者都等于 10em，所以量一次两边通用。
在 `onReady` 里用离屏画布把标题量准后写进 `titleLines`，预览逐行渲染、画布直接用同一份，
「预览即成品」这条继续成立。量不到（画布未就绪）就退回原来的行为，不会白屏。

`balanceLines` 是纯函数，补了 8 组断言（孤字、行数不变、不吞字、超上限时保持贪心、
空串与坏参数不抛），跑在 `test:miniapp-utils` 里。

---

---

## 24. 图标视觉居中、首页配色、登录表单宽度（V1.14）

### 24.1 首页功能入口图标没对齐 ⚠️ 真 bug，已修

五个格子（`.ico`）都是 100rpx、`align-items:center`，**框是对齐的**；
错位发生在 24×24 的 viewBox **内部**——图标是手写路径，各自的墨迹落点并不一致。

在真实 SVG 引擎里逐个 `getBBox`（含描边宽度）量出来：

| 图标 | 墨迹中心 y | 相对 viewBox 中心 |
|------|-----------|-------------------|
| `entry-news` 书院动态 | 12.0 | 0 |
| `museum` 展馆展示 | 13.0 | **+1.0** |
| `course` 课程中心 | 10.5 | **−1.5** |
| `entry-resource` 资源下载 | 12.0 | 0 |
| `entry-enroll` 报名 | 12.5 | +0.5 |

同一排里最高与最低差 **2.5 个 viewBox 单位**，图标实际渲染 50rpx，
折算就是 **5rpx** 的高低差——肉眼能看出来。tabBar 那排是同样两个图标，同样的毛病。

全站 46 个图标量了一遍，**17 个墨迹不居中**。改法是给 `icons.js` 的图标加一个
`c: [dx, dy]` 修正量，由 `buildSrc` 通过挪 viewBox 原点实现
（`viewBox="-dx -dy 24 24"`）——**不改路径字符串**，改错也只会平移、不会把图形改形。

`play` 是唯一的**刻意例外**：右向三角形的视觉重心本就该偏右于几何中心，
它在圆形播放键里按墨迹居中反而看着偏左，那 +1.5 是对的，别去"修正"。

配套两件事：

- **`scripts/measure-icon-centering.mjs`**（手动跑，需要 Playwright，不进 preflight）：
  在真实 SVG 引擎里重新量一遍并打印建议的 `c` 值。新增图标后跑一次填回去即可。
  现在跑出来是「墨迹中心全部落在 (12,12) 的 ±0.25 内」。
- **`check-wxml` 加检查 F**：体检"机制还在、数值没写飞"——`buildSrc` 必须仍在应用 `c`，
  且每个 `c` 的绝对值不超过 3。真正的居中测量要跑 SVG 引擎，纯 node 的关卡做不到，
  所以这里只拦机制被删掉这种情况。反向验证过。

### 24.2 首页配色评价

**先说成立的部分**：navy + gold + paper 这套色系与登录页、印章是一致的；
白卡片 + `#F5F8FC` 底 + 柔和投影读起来干净；顶部那幅书法字是很强的品牌资产。

**改了一处**：五个入口原来是「四蓝 + 一金」。金色是全站的强调色——印章、徽记、
下面那张「精品好物」大卡都用它。放在五个**平级**入口中的一个上，
会让「报名」看着像主操作，而它并不是；同一屏里两处大面积金色也互相削弱。
已把第五格改成同族蓝 `#5C79C8 → #34479B`。

明度上特意取 e1~e3 那一档、没有继续往浅了排：`e4` 的 `#6E8FCB` 配白色图标
对比度已经是这排里最低的（约 3.3:1），再浅就该看不清了。
改后 e5 约 4.4:1，比 e4 还稳。

这是一行 WXSS，想把金色换回去很容易。

**没改、但值得记下的观察**：

1. **e1~e4 四个蓝彼此太近**（`#3F57B5 / #4E7CC4 / #5867B8 / #6E8FCB`），
   在 100rpx 的尺寸上几乎分不出来。要么就让它们**完全一样**、靠图标区分（更克制）；
   要么把跨度拉开。现在这样"差一点点"，看着像没调准而不像有意为之。
2. **同屏出现了三套色彩逻辑**：入口区（蓝色阶）、精品好物（金）、线上展馆卡（蓝 / 青 / 红）。
   展馆卡的三色是内容分类色，和入口区的色阶不是一个体系。不是错，但整屏略显散。
3. **首屏轮播现在是纯色块**。后台没配图时轮播只有深蓝渐变 + 文字，
   和下面的入口卡颜色接近，整屏"蓝上加蓝"。这是内容问题不是代码问题，
   但**提审前必须配真实图片**，否则首屏第一眼就是一块深蓝。
4. **顶部搜索框对比偏弱**：白底 header 上放 `#F2F7FC` 的搜索框，边界几乎看不见。
   加一道 `--line` 的描边会清楚很多。

### 24.3 登录页表单宽度

原来 `.login-inner` 左右内边距 60rpx，表单宽 630rpx = **屏宽的 84%**，
几乎顶到屏幕两边；而上方印章 248rpx、主标题「云端书院」墨迹约 312rpx，
横向重心明显不在一条轴上。

渲染了四档对比后取 **76rpx（表单 598rpx，占屏宽 80%）**：

| 内边距 | 表单宽 | 占屏宽 | 表单 / 主标题 |
|--------|--------|--------|---------------|
| 60（原） | 630rpx | 84% | 1.91 |
| **76（本次）** | **598rpx** | **80%** | **1.81** |
| 90 | 570rpx | 76% | 1.73 |
| 104 | 542rpx | 72% | 1.64 |

再窄（90 / 104）按钮开始显小，主操作不该那么收敛。
80% 这一档下按钮在 iPhone 13 上仍有 311pt 宽，触控尺寸绰绰有余。

顺带回答"是不是把输入框和按钮改成同宽了"：**它们本来就是同宽的**（都是 `width: 100%`）。
上一轮改的是"按钮在宽屏机型上被基础库收窄"那个 bug，让它恢复成本来该有的通栏宽度，
不是新把两者拉齐。

### 24.4 登录页还可以做但本轮没做的

- **输入框缺少聚焦态**。点进去只有光标，边框没有任何变化。加一圈金色描边会明显好用，
  但 WXSS 对 `<input>` 没有 `:focus`，得靠 `bindfocus` / `bindblur` 切类，
  属于加状态、不是纯样式，留待确认后再做。
- **输入框左内边距 92rpx 是写死的**，和图标的位置（`left: 30rpx`）、尺寸没有推导关系。
  以后改图标尺寸，文字起点就会和图标对不齐。目前是对的，记一笔。

---

---

## 25. UI 体检：对比度、触控尺寸、登录页收尾（V1.15）

这一轮没有等新截图，而是**量了两件能量的事**：全站文字对比度、所有可点元素的触控尺寸。
下面按"改了 / 没改"分开列。

### 25.1 §24 留的两项，判断与结论

**① 输入框聚焦态 —— 做了。**

原本我的顾虑是"别的页面都没有聚焦态，只给登录页加算不算不一致"。想清楚了：
其它页的输入框在白底上、边框本来就看得见；登录页是**深色底 + 两个长得一模一样的框**，
点进去只有一个光标可辨认。需求不同，做法不同才是对的，不是不一致。

描边用印章那支金色（`rgba(201,162,39,.62)`）并加一层内发光，
不引入新颜色。五个输入框（登录 2 + 改密 3）都接上了。

一个细节：失焦时**只清掉"还是自己"的那一次**。两格之间切换时，
`blur` 与 `focus` 的先后在不同基础库上并不一致，无条件清空会把刚聚上的那格一起熄掉。

**② 输入框左内边距 92rpx 写死 —— 没改。**

它确实和图标位置没有推导关系，但现在的值是对的，改成变量并不能防止以后改错，
只是把一个硬编码换成另一个。不值当，继续记在这。

### 25.2 文字对比度 ⚠️ 系统性问题，已改

按 WCAG 2.1 AA（普通文字 ≥4.5:1）逐组算：

| 组合 | 改前 | 改后 |
|------|------|------|
| `--muted #8A93B2` 于白卡 | **3.04** | `#67708C` → **4.91** |
| `--muted` 于 `--paper` | **2.86** | → **4.61** |
| tabBar 未选中标签 | **3.04** | → **4.91** |
| 登录 输入占位 | **3.78** | 透明度 .55→.70 → **5.12** |
| 登录 分隔文字 | **3.78** | .55→.70 → **5.12** |
| 登录 英文名 | **3.68** | .7→.85 → **4.65** |
| 登录 页脚次行 | **3.11** | .6→.85 → **4.65** |

`--muted` 是全站的提示文字、tabBar 未选中标签、空态说明——恰恰是
"看不清就真的看不见"的那一类，共 103 处引用（56 处 `var(--muted)` + 47 处硬编码），
硬编码的一并换掉了。深色底那几处只调透明度、不动色相，**版面一点没变**。

层级仍在：`--ink` 14.85 › `--sub` 5.79 › `--muted` 4.91。muted 与 sub 是拉近了一点，
但 sub 用在更大的字号和更重的字重上，实际不会混。

**新增关卡 `check:contrast`**：从 `app.wxss` 读调色板 token，逐组算与实际背景的对比度，
低于阈值即报错。反向验证过——把 `--muted` 调回 `#8A93B2`，两条立刻红。
页面里零散写死的字色查不了，只能靠 review，但 token 一旦被"调浅一点更好看"，这里会拦住。

### 25.3 触控尺寸

按 44pt（88rpx）这条通行下限扫了全部带 `bindtap` 的元素，**16 个偏小**。改了最小的两个：

| 元素 | 改前 | 改后 |
|------|------|------|
| 搜索框清空键 `.sclear` | 40rpx（20pt） | 盒子放到 64rpx（32pt），图标不变，右侧负边距抵消 |
| 反馈图片删除键 `.fb-img-del` | 40rpx（20pt） | 圆点 48rpx + 透明 `::after` 外扩到 72rpx（36pt） |

删除键**特意没做更大**：误删比难点更糟。缩略图是 168rpx，72rpx 的热区占角上不到一半，
不会抢走看图的点击。

**顺带修掉一个语义错误**：搜索框的清空键此前借用 `eye-off` 图标——那是一只被划掉的眼睛，
"隐藏"的意思，出现在清空位置上会让人以为是密码显隐。补了一个 `close` 图标（第 47 个），
换上去了。

**其余 14 个没改**，都在 28~42pt 之间（顶栏图标 64rpx、返回键 66~68rpx、
收藏/下载 72rpx、发送键 76~84rpx 等）。它们的视觉尺寸是刻意的，
放大热区要么改动观感、要么依赖 `::after` 这类技巧，逐个都得单独判断，
不适合一次性批量动。清单记在这里，后面可按页处理。

### 25.4 还看得见、但本轮没动的

按"值不值得改"排序：

1. **首页轮播没有真实图片**。后台没配图时轮播只是深蓝渐变 + 文字，和下面的入口卡颜色接近，
   首屏"蓝上加蓝"。这是**内容**问题不是代码问题，但它决定了审核员看到的第一眼。
   提审前必须配图——这条的优先级高于本节其它所有视觉项。
2. **e1~e4 四个蓝彼此太近**（`#3F57B5 / #4E7CC4 / #5867B8 / #6E8FCB`），
   100rpx 尺寸下几乎分不出来。要么让它们完全一样、靠图标区分，要么把跨度拉开；
   现在"差一点点"，看着像没调准。
3. **顶部搜索框对比偏弱**：白底 header 上放 `#F2F7FC` 的搜索框，边界几乎看不见，
   加一道 `--line` 描边会清楚很多。
4. **密码框没有显隐切换**。改密页有**三个**密码框，全程盲打，输错只能重来。
   `eye` / `eye-off` 图标现成的（`eye-off` 从清空键上撤下来后正好空出来）。
   这算加功能不是改样式，等确认。
5. **同屏三套色彩逻辑**：入口区色阶、精品好物金卡、展馆卡的蓝/青/红。不是错，但整屏略散。

---

---

## 26. 入口色阶、搜索框边界、密码显隐（V1.16）

### 26.1 五个入口的色阶：等对比度色相扫描

要求是"不要完全统一，把跨度拉开，但五种颜色又怕花里胡哨"。做法是**只让色相变，不让分量变**。

先说为什么不能直接扫色相：HSL 的 `L` 不等于人眼感知亮度。同样 `L=48%`，
青色比紫色亮得多，纯粹扫色相会让越靠后的格子里白色图标越淡——
实测那一版最后一格只剩 3.62:1，第一格却有 7.34:1。

改成**按"白色图标落在它上面的对比度"反推明度**：逐格二分找 L，五格统一钉在 **6.2:1**。
色相从 **238° 走到 198°**（40° 跨度，由品牌蓝起、到深青止，全程仍在冷色一族）。

| 格 | 渐变 | 白图标对比度 |
|----|------|--------------|
| 书院动态 | `#5256B8 → #383B90` | 6.21 |
| 展馆展示 | `#475CAE → #324280` | 6.16 |
| 课程中心 | `#3F619A → #2A436D` | 6.20 |
| 资源下载 | `#39648B → #24435D` | 6.23 |
| 报名 | `#33687E → #1F4150` | 6.14 |

（旧值是 6.48 / 4.20 / 5.19 / **3.25** / 4.18 —— 分量本来就不齐，第四格最淡。）

比过四个候选：目标 5.2 那版整体偏亮、首格偏紫；跨到 250°→194° 那版两端太艳；
同色相只拉明度那版中间三格仍分不开。238°→198° + 6.2 这组首格贴着品牌蓝、末格是深青，
既看得出五格不同，又不会散成一堆杂色。

⚠️ 这五个值是算出来的，不是调出来的。要改任一格，请重新跑等对比度二分，别手拨。

### 26.2 搜索框边界

**先更正我上一轮的说法**：我当时说"加一道 `--line` 描边会清楚很多"——
**描边其实一直都在**（`border: 1rpx solid #E7ECF6`）。真正的问题是
填充 `#F2F7FC` 与白色 header 只差 **1.08:1**、描边只差 **1.18:1**，两个颜色都太接近白，
所以整个框几乎看不出边界，不是"没画边"。

改成填充 `#EAF0F9`（1.15）+ 描边 `#D3DCEC`（1.38）。渲染比过三档：
只加深描边（1.38）改善有限；描边推到 `#C6D1E6`（1.54）就变成"描边输入框"那种硬边，
和这套柔和设计不搭。填充与描边各加深一档最合适。

按下态跟着从 `#EDF3FB` 调到 `#E1E9F5`，仍比常态深（`check:press-feedback` 会验）。
搜索页的 `.sbox` 是同样的处境（坐在近白的 `.sbar` 上），用同一组值；
没有去动 `--sky-3` / `--line` 这两个 token —— 它们在别处（浅底分隔线）是合适的。

不追求 WCAG 1.4.11 的 3:1：那条对"边界是识别该控件所必需"的组件才适用，
而搜索框有放大镜图标 + 占位文字，3:1 的边框在这里只会显得笨重。

### 26.3 密码显隐

四个密码框（登录 1 + 改密 3）都加了右侧眼睛键。改密页那三格尤其需要——
"新密码 / 确认密码"往往要同时看着比对，所以**每格各自一个开关**，不是共用一个。

几何上与左侧图标严格镜像：`.field-ic` 是 `left:30rpx` + 宽 40rpx，中心距左 50rpx；
`.field-eye` 是 `right:30rpx` + `margin-right:-16rpx` + 宽 72rpx，中心距右也是 50rpx。
输入框的 `padding-right` 同步给到 92rpx，与左侧对称。
眼睛键盒子做到 72rpx（36pt），图标仍是 18 —— 靠留白撑开可点范围，观感不变。

图标语义：隐藏时显示 `eye`（点一下看看），显示时显示 `eye-off`（点一下藏起来）。

**一个已知行为**：点眼睛会让输入框失焦（小程序里点任何 view 都会），
于是聚焦态那圈金边会消失。没有去做"切完自动聚回来"——`focus` 属性得靠 false→true
翻转触发，和 blur 清理逻辑绕在一起容易把键盘搞出闪烁；而显隐通常是打完或打之前
点一下，不是边打边点。权衡后选了简单可靠的那条。

---

## 27. 触控热区补至 44pt、tabBar 色与海报辅助字（V1.17）

### 27.1 三处高频小操作扩到 88rpx

Review 指出 §25.3 改过的密码显隐（72rpx）、搜索清空（64rpx）、反馈删除（72rpx 热区）
仍低于移动端常用 **44pt（88rpx）** 舒适下限。本轮只扩**透明热区**，视觉图标尺寸不变：

| 元素 | 改前热区 | 改后热区 | 备注 |
|------|----------|----------|------|
| `.field-eye` | 72rpx（36pt） | **88rpx** | `right:22` + `margin-right:-16` 保持图标中心距右 50rpx |
| `.sclear` | 64rpx（32pt） | **88rpx** | 图标仍 14，负边距 -18rpx 抵消布局 |
| `.fb-img-del` | 48+12×2=72rpx | **48+20×2=88rpx** | 可见圆点仍 48rpx，::after 外扩 |

新增 **`check:touch-targets`** 钉住这三处与 `app.json tabBar.color`，已进 preflight / CI。

### 27.2 配置与字面量同步

- `app.json` 的 `tabBar.color` 由 `#8A93B2` 改为 `#67708C`（与 custom-tab-bar 运行时一致，去掉配置残留）。
- 海报预览 `.p-qr-sub` 由 `#A9B2C8` 改为 `#67708C`，与 `.p-qr-hint` 及全站 muted 一致。

---

*文档版本：V1.17 · 2026-08-23 · 触控热区 44pt、tabBar 色、海报辅助字（§27）*
---

## 28. 对 §27 那批改动的复核（V1.17）

三个提交都看了，**方向都对**，其中展馆播放器那个 bug 找得准。下面是复核结果与三处修正。

### 28.1 反馈删除键：账面 88rpx，实际只有 74rpx ⚠️ 已修

`.fb-img-del::after` 四边各外扩 20rpx，账面热区 48 + 20×2 = 88rpx（44pt）。
但父级 `.fb-img-cell` 是 **`overflow: hidden`**，超出 cell 的那截既不显示、**也收不到点击**。
元素本身距上/右各 6rpx，所以往外实际只扩得了 6rpx：

    有效热区 = min(外扩 20, 偏移 6) + 48 + 内扩 20 = 74rpx（37pt）

—— 没到 44pt。（改前是 min(12,6)+48+12 = 66rpx，所以那次确实有改善，只是没到位。）

改成**不对称外扩**：外侧两条（`top` / `right`，元素贴的是右上角）各 -6rpx 正好贴到 cell 边，
内侧两条（`bottom` / `left`）各 -34rpx，合计 6 + 48 + 34 = **88rpx**，整块都落在 cell 内。
内侧只扩到刚好 88rpx 为止——缩略图本身绑了预览点击，删除区越大越容易误删。

> 写的时候自己也踩了一次坑：第一版把纵向的内外侧写反了（`top:-34 / bottom:-6`），
> 热区变成 88×60。是新关卡把它拦下来的。

**关卡跟着改**：原来按 `base + inset×2` 算会高估。现在按
`min(外扩量, 距 cell 边的偏移) + 圆点 + 内扩量` 算，并额外断言
`.fb-img-cell` 仍是 `overflow: hidden`（这个前提一旦变了，换算就得重来）。
反向验证过：把 `::after` 改回对称 -20rpx，关卡报「有效热区 74×74rpx」。

### 28.2 `.p-qr-sub` 变成了空规则 —— 已清理

`.p-qr-sub` 由 `#A9B2C8` 调到 `#67708C`（对比度 2.3 → 4.91，改得对），
但 `.p-qr-hint` 本来就是 `#67708C`，于是这条规则不再产生任何效果。
留着一条不起作用的规则，下次很容易被人「顺手调浅一点」再把对比度破坏掉。
已删除规则与 WXML 上的多余类名，理由写在 `.p-qr-hint` 的注释里。

（两行的层级靠行序与内容区分即可——这块只是保存前的占位板，真二维码生成后就被替换。）

### 28.3 `settleCameraDamping` 已成死代码 —— 已删除

它是 `74c9789` 为 E2E 加的等待函数，`d879286` 换成注入固定 delta 的确定性写法后就没人调了
（全仓只剩一句注释提到它）。

而且它本身也做不到宣称的事：`dampingFactor = 0.08`，每次 `update()` 衰减 `×0.92`；
一次 90px 拖曳产生的 `_sphericalDelta.theta` 约 0.64 rad，
要衰减到函数里那个 `1e-5` 的阈值需要约 **132** 次，而 `maxFrames` 默认 **120** ——
也就是说它在自己被写来应付的那个场景里**必然返回 false**，只是没人看返回值。

死代码 + 达不到宣称行为，删掉比留着好。

### 28.4 确认没问题的部分

- **`clearControlsInertia` 的诊断与修法正确。** `refit` 直接改 `camera.position` 后调
  `controls.update()`，而 OrbitControls 的 `_sphericalDelta` 还留着上一次拖曳未衰减完的量，
  下一帧会被叠加进方位角。在本机跑了这批 E2E：**7/7 通过**；
  再把 `clearControlsInertia()` 调用临时摘掉重跑，新用例如期失败，
  报 `delta.theta = 0.046`（= 注入的 0.05 × 0.92，正好是一次 update 的衰减量）——
  说明这条用例真的钉住了这个 bug，不是摆设。
- **`player.html` 与 `player.bundle.js` 没有脱节。** 新增的四个测试 hook 只进了 html、
  没进 bundle —— 这是**对的**：那段包在 `/* TEST-HOOKS-START — build-viewer 剔除 */` 里，
  构建会剥掉。`node build-viewer.mjs --check` 报
  「player.view.html + player.bundle.js OK (byte-identical + semantics)」，已核实。
- **`app.json` 的 `tabBar.color` 同步到 `#67708C`** 是我上一轮漏的：自定义 tabBar 组件的颜色改了，
  但 `app.json` 里那份（自定义 tabBar 加载失败时的原生兜底）还是旧值。补得对。
- `clearControlsInertia` 里清 `_panOffset` 在本项目其实是空操作（`enablePan = false`），
  但作为防御性写法没有害处，未动。

---

*文档版本：V1.17 · 2026-08-23 · 复核 §27 批次：删除键热区被父级裁切、空规则清理、死代码删除（§28）*
*文档版本：V1.16 · 2026-08-22 · 入口等对比度色相扫描、搜索框边界、密码显隐（§26）*
*文档版本：V1.15 · 2026-08-22 · UI 体检：文字对比度、触控尺寸、登录页聚焦态（§25）*
*文档版本：V1.14 · 2026-08-22 · 图标视觉居中、首页入口配色、登录表单宽度（§24）*
*文档版本：V1.13 · 2026-08-22 · 宽屏适配复核、图标单位修复、海报标题均衡折行（§23）*
*文档版本：V1.12 · 2026-08-22 · 助手浮层遮挡、海报分隔符、后台内部措辞（§22）*
*文档版本：V1.11 · 2026-08-22 · 数据层默认文案与 demo 内容、导航栏统一、登录页留白（§19–§21）*
*文档版本：V1.10 · 2026-08-22 · 个人中心机构名、海报封面降级、登录页留白（§18）*
*文档版本：V1.9 · 2026-08-22 · 印章重做为圆角方框，学院与关联小程序更正（§17）*
*文档版本：V1.8 · 2026-08-22 · 主体归属落地、篆印替换与 demo 中性化（§16）*
*文档版本：V1.7 · 2026-08-21 · 公司主体备案路径复审（§15）*
*文档版本：V1.6 · 2026-08-21 · 追加 UI 视觉第六轮（§14）与按下反馈关卡*
*文档版本：V1.5 · 2026-08-20 · 追加 UI 视觉第五轮（§13）与后台侧栏图标关卡*
*文档版本：V1.4 · 2026-08-20 · 追加 UI 视觉第四轮（§12）*
*文档版本：V1.3 · 2026-08-20 · 追加 UI 视觉第三轮（§11）*
*文档版本：V1.2 · 2026-08-20 · 追加 UI 视觉第二轮（§10）与 preflight 关卡*
*文档版本：V1.1 · 2026-08-20 · 追加 UI 视觉补充轮（§9）*
*文档版本：V1.0 · 2026-07-30 · 对应小程序文案备案调整提交*
