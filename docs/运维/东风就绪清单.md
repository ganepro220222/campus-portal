# 东风就绪清单

> 目标：**本地代码/脚本/文档闭环**，云资源与甲方资料到位后「填表 → 跑脚本 → 部署」即可。  
> 最后更新：2026-09-01 · 过渡域、微信域名、OSS+CDN、ASR 预发实测已复核（进度见 [项目进度与里程碑](../项目进度与里程碑.md)；实机路径见 [预发实测记录](../测试/预发实测记录_20260901.md)）

## 1. 发布前本地预检（无需云）

```bash
npm run preflight:local
# 串联：test:deploy-env + check:deploy-env + check:wxml + check:admin-menu-icons
#       + check:press-feedback + test:miniapp-utils + mock-guard + sync-navigate-appids + release-gate
# Docker 已启动时：npm run smoke:api
```

云资源与 `.env` 填好后：

```bash
npm run check:release-all -- --env .env
cd backend && mvn test          # 需 Java 17
cd admin && npm run typecheck && npm run build
```

## 2. 代码就绪 vs 东风待填

| 模块 | 代码状态 | 东风（外部依赖） | 2026-08-30 |
|------|----------|------------------|------------|
| 后端 API + 管理端 | ✅ 就绪 | 正式域切到专用小程序域名 | ✅ 过渡域 `https://api.yunmanvr.com`（admin 同源 `/admin/`） |
| 微信小程序登录 | ✅ code2session | 生产 AppID / Secret | ✅ 已在 ECS 配置（勿入库） |
| 订阅消息（报名） | ✅ 骨架 | 模板 ID（**不含活动提醒**） | ✅ `GET /api/v1/subscribe/templates` 已返回两条 |
| 关联小程序 jump | ✅ 首页横滑 + 个人中心 + 列表 | 兄弟小程序 AppID 清单 | ⏳ 仍待甲方；`navigateToMiniProgramAppIdList` 为空 |
| 关联 api_sync | ⏳ **预留可见** | 对方 API 文档 + 密钥 | ⏳ V1.0 不做真实拉取 |
| 海报小程序码 | ✅ getwxacode + dev 降级 | 生产 WX 凭证 | ✅ 与现用 AppID 同一套 |
| 课程字幕 ASR | ✅ 阿里云 filetrans 4.0 + 轮询 Job | ASR Key / AppKey | ✅ 预发已写入并现场生成字幕（试用约 92 天）；无 Key 仍可手动上传 |
| AI 问答 | ✅ 关键词 MVP | 可选智谱 Key | ⏳ 非阻塞 |
| OSS 媒体 | ✅ 主链 | Bucket + CDN | ✅ Bucket `yunman-shuyuan`（成都、私有）；CDN `cdn.yunmanvr.com`（HTTPS / 私有回源 / 1TB 包 / 200Mbps 封顶 / QUIC）。资料通过签名 CDN Range 分块直读，API 仅作续传回退 |
| 微信服务器域名 | ✅ 按接口走 | 公众平台勾选 | ✅ request=`api.yunmanvr.com` + `cdn.yunmanvr.com`；uploadFile=`api.yunmanvr.com`；downloadFile=OSS 原站 + CDN |
| 微信业务域名 | ✅ web-view | 720 子域 | ✅ `720roma` / `720yun` 租户子域 + `www.yunmanvr.com` |
| staging 部署/压测 | ✅ | — | ✅ 2026-08-27 压测 + 备份演练；08-31～09-01 媒体实机见实测记录 |

## 3. 东风到位后 30 分钟流程

```bash
# 1. 生产配置
cp .env.prod.template .env
cp miniapp/config/env.prod.template.js miniapp/config/env.js   # ENV=prod + 域名
cp miniapp/config/navigate-appids.template.json miniapp/config/navigate-appids.json

# 2. 编辑 .env / env.js / navigate-appids.json 填入真实值

# 3. 小程序跳转白名单
node scripts/sync-navigate-appids.js

# 4. 预检
npm run preflight:local
npm run check:release-all -- --env .env

# 5. 部署 — 见 docs/运维/部署手册_V1.0.md §5.3
```

## 4. 关联小程序四种方式（管理端均可配置）

| 方式 | 小程序端 | 未对接时行为 |
|------|----------|--------------|
| manual | 弹窗简介 | 正常展示 |
| jump | `navigateToMiniProgram` | AppID 错/未白名单 → Toast 跳转失败 |
| embed_h5 | web-view | 无 URL → Toast |
| api_sync（预留） | Modal 说明预留 | **不隐藏**；待甲方 API 后实现拉取 |

**说明：** Swagger 描述的是「别人调我们」；`api_sync` 是「我们调别人」，方向相反。

## 5. V1.0 明确不做

- `activity_remind` 活动开始提醒订阅消息
- `api_sync` 真实拉取逻辑（仅预留配置与提示）
- AI 向量检索（维持关键词 MVP）

## 6. 相关文档

- [ASR 字幕开通步骤](./ASR字幕开通步骤.md)
- [环境变量说明](./环境变量说明.md)
- [部署手册](./部署手册_V1.0.md)
- [上线分工 checklist](./上线分工checklist_V1.0.md)
- [项目进度与里程碑](../项目进度与里程碑.md)
