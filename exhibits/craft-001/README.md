# 旗舰样片 · 珐琅彩缠枝牡丹纹瓶（增强版 3D 鉴赏）

对方案 `docs/三维展示系统/增强版3D鉴赏系统_实施方案_v2.md` 的**可运行样片**，用于向合伙人/甲方演示效果。
纯前端静态展包，自包含（含 `vendor/` 的 Three.js，**不依赖任何外部 CDN**）。

## 本地运行（给对方看效果）
```bash
cd exhibits/craft-001
python3 -m http.server 8099
# 浏览器打开 http://127.0.0.1:8099/
```
> 必须用「静态服务器」打开，不能直接双击 `index.html`（ES 模块 + fetch 需 http 协议）。
> 建议用桌面 Chrome / 手机浏览器体验；展馆大屏效果最佳。

## 两种模式
| 模式 | 入口 | 谁用 | 能力 |
|---|---|---|---|
| 观看版（默认） | `http://…:8099/` | 终端用户 | 自动旋转、热点显隐、点击热点看讲解、重置、全屏、中/英、展厅光/细节光 |
| 编辑版 | `http://…:8099/?mode=edit` | 开发方 | 十面板轻量编辑器（见下） |

### 编辑版面板（接近方案 v2 第 5 节）
- **资产**：从**任意 URL 加载模型**（相对/绝对；跨域需 CORS）、拖拽/选择本地 `.glb` 预览、显示当前模型路径。
- **模型 Transform**：缩放 / 适配尺寸 / 位移 XYZ / 旋转 Y°，可重置。
- **相机**：FOV / 自动旋转速度 / 最近最远距离 / **保存当前视角为默认**。
- **材质**：全局 曝光 / 环境光强 / 金属度 / 粗糙度；**按材质名分组覆盖**（自动列出模型材质名）。
- **灯光（四光源）**：环境 / 主 / 补 / 轮廓光 的强度与颜色。
- **环境 IBL**：可见全景背景开关、换全景图 URL。
- **热点**：Shift+点击模型表面新增；逐条改标题/文案、删除、更新聚焦机位。
- **预设**：从当前状态新建 / 删除。
- **性能**：FPS / draw calls / 三角面 / 顶点 / 材质 / 贴图 / DPR，超阈值告警。
- **导出**：导出 `config.json`、截取当前帧为 `poster.jpg`、校验 + 部署清单。

> 关于「模型放哪」：默认样片是**同目录静态包**（`assets/model.glb`，最省心、免 CORS）。但 `config.json` 的 `assets.model` 可填**任意 URL**——编辑版「资产 → 从 URL 加载」即演示了这点；跨域资源需服务器配 CORS。

## 已演示的方案要点
- **真实全景 IBL**：`assets/panorama.jpg`（由 `test/4/BJ.jpg` 50MB 压到 **497KB / 2048×1024**）经 PMREM 作环境反射，釉面高光"活"起来（无全景时自动降级 RoomEnvironment）。
- **配置驱动**：所有模型/相机/材质/热点/文案/预设都在 `config.json`，一份 `index.html` 通用。
- **热点讲解**：点击热点 → 暂停旋转 + 相机平滑聚焦 + 讲解卡片（PC 右侧栏 / 手机底部抽屉）+ 双语；转到背面自动淡化。
- **加载编排**：poster 占位 → 模型淡入 → 全景 IBL 后台接管；WebGL 不支持 / 加载失败有降级与重试。
- **交付闭环**：开发方在编辑版调参打点 → 导出 `config.json` → 替换展包 → 上传静态服务器。校方不接触 GLB / 材质 / 热点。

## 新增一件展品
```
exhibits/craft-002/
  index.html      # 复制本目录（播放器通用）
  vendor/         # 复制（或软链到共用版本）
  config.json     # 改这一份：标题/模型/相机/材质/热点
  assets/         # 换 model.glb + panorama.jpg + poster.jpg
```

## 上线注意（详见方案 v2 第 10/12 节）
- `vendor/` 已自托管 Three.js；生产**观看版不要打包编辑代码**（编辑版单独部署 / 加访问控制）。
- 部署到静态目录（如 `https://shuyuan.gzcpu.edu.cn/exhibits/craft-001/`），VR 平台热点配外链指向它。
- 资源建议带版本号或 hash 以避免 CDN/浏览器缓存。
