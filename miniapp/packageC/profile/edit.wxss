/* packageC/profile/edit.wxss */
.pe-page { height: 100vh; display: flex; flex-direction: column; background: #F5F8FC; }
.pe-state {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 24rpx;
  color: #67708C;
  font-size: 28rpx;
}
.pe-retry {
  padding: 12rpx 28rpx;
  font-size: 26rpx;
  color: var(--sky);
}
.pe-scroll { flex: 1; overflow: hidden; padding: 24rpx 0 0; }

.pe-card {
  margin: 0 28rpx;
  background: #fff;
  border: 1rpx solid var(--line);
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: var(--shadow-sm);
}
.pe-hint {
  font-size: 24rpx;
  color: var(--sub);
  line-height: 1.6;
  margin-bottom: 24rpx;
  padding: 18rpx 20rpx;
  background: var(--sky-3);
  border-radius: 16rpx;
}
.pe-label { font-size: 27rpx; font-weight: 600; color: var(--ink); margin-bottom: 14rpx; }
.pe-label:not(:first-of-type) { margin-top: 26rpx; }
.req { color: var(--color-danger); margin-left: 4rpx; }

.pe-input {
  width: 100%;
  height: 84rpx;
  background: var(--sky-3);
  border: 1rpx solid var(--line);
  border-radius: 18rpx;
  padding: 0 22rpx;
  font-size: 27rpx;
  color: var(--ink);
  box-sizing: border-box;
}

.pe-pad { height: 40rpx; }
.pe-bar {
  padding: 20rpx 28rpx calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  border-top: 1rpx solid var(--line);
}
.pe-submit {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 88rpx;
  border-radius: 44rpx;
  background: linear-gradient(135deg, #3F57B5, #2B356E);
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
  letter-spacing: 2rpx;
  border: none;
  box-shadow: 0 12rpx 30rpx rgba(43, 53, 110, 0.32);
}
.pe-submit::after { border: none; }
.pe-submit.ing { opacity: 0.7; }
